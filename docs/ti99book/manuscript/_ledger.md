# `_ledger.md` — Terminology, Address, and Fact Ledger
*Every address, symbol, term of art, date, and figure the manuscript asserts, logged at first assertion. Later chapters must match. Loaded every session.*

| Entry | Value as asserted | First asserted | Notes |
|---|---|---|---|
| GSI founded / renamed TI | 1930 / 1951 | Ch. 1 §1.1 | |
| First commercial silicon transistor (TI, Teal) | 1954 | Ch. 1 §1.1 | |
| Regency TR-1 transistor radio | 1954 | Ch. 1 §1.1 | with I.D.E.A. |
| Kilby demonstrates integrated circuit | Sept 1958 | Ch. 1 §1.1 | Nobel 2000 |
| Handheld calculator prototype ("Cal Tech") | 1967 | Ch. 1 §1.1 | Kilby, Merryman, Van Tassel |
| TI Datamath (TI-2500) ships | 1972 | Ch. 1 §1.1 | |
| Speak & Spell introduced | 1978 | Ch. 1 §1.1 | Breedlove, Wiggins, Frantz, Brantingham; LPC |
| TMS9900 introduced | 1976 | Ch. 1 §1.2 | "among the first single-chip 16-bit CPUs" |
| TMS9985 cancellation forces 9900 into 99/4 | as documented account (Guttag et al.) | Ch. 1 §1.3 | root of 8-bit multiplexed bus |
| TI-99/4 launch | June 1979 announce; ships fall 1979; $1,150 with monitor | Ch. 1 §1.3 | monitor = rebadged 13" Zenith color set |
| 99/4 keyboard | 41-key calculator-style ("chiclet") | Ch. 1 §1.3 | hedged "41-key" — verify in tech-audit pass |
| GROM/cartridge control strategy | "Solid State Software" Command Modules | Ch. 1 §1.4 | lockout politics → Ch. 25 sidebar |
| TI-99/4A launch | June 1981, $525 (no monitor) | Ch. 1 §1.5 | 9918A adds bitmap mode |
| Console v2.2 ROM-cart lockout | 1983 | Ch. 1 §1.4/§1.6 | aimed at unlicensed ROM-only carts (Atarisoft era) |
| QI ("Quality Improved") beige console | 1983 | Ch. 1 §1.5 | cost-reduced final revision |
| VIC-20 price war entry | $299 (1981) | Ch. 1 §1.6 | Tramiel: "Business is war" (widely attributed) |
| 99/4A price path | $525 → ~$299 (spring '82) → $100 rebate (fall '82) → ~$150 (early '83) → $99 (mid-'83) → $49 clearance (late '83) | Ch. 1 §1.6 | street prices; hedged per R-2 |
| Bill Cosby TI spokesman fee | reported seven figures/yr | Ch. 1 §1.6 | R-2 hedge |
| TI Q2 1983 loss | reported ≈ $119M (quarter) | Ch. 1 §1.6 | June 10, 1983 warning cratered the stock |
| Black Friday | Friday, October 28, 1983 | Ch. 1 §1.7 | exit announced with Q3 results |
| Exit write-off | reported ≈ $330M pre-tax | Ch. 1 §1.7 | R-2 hedge |
| TI full-year 1983 | first annual loss in company history (reported ≈ $145M) | Ch. 1 §1.7 | R-2 hedge |
| 99/2 | announced Jan 1983 at $99, cancelled mid-1983 | Ch. 1 §1.7 | |
| 99/8 "Armadillo" | cancelled at Black Friday, prototypes survive | Ch. 1 §1.7 | full treatment Ch. 44 |
| Total 99/4(A) consoles | commonly cited ≈ 2.8 million | Ch. 1 §1.7 | R-2 hedge |
| 99'er → Home Computer Magazine rename | early 1984 | Ch. 1 §1.8 | hedged |
| Home Computer Compendium → MICROpendium | first issue Feb 1984; ran to 1999 | Ch. 1 §1.8 | Burns & Koloen |
| First Chicago TI Faire | weeks after Black Friday, Nov 1983; still annual | Ch. 1 §1.8 | |
| TI Intern | Heiner Martin's ROM/GROM disassembly book (mid-1980s, Germany) | Ch. 1 §1.8 | canonical reference, used from Ch. 22 on |
| Myarc Geneve 9640 | 1987; TMS9995 | Ch. 1 §1.8 | survey in Ch. 44 |
| GRAM Kracker (Millers Graphics) | mid-1980s GRAM device | Ch. 1 §1.8 | enables user GPL → Ch. 25 |
| Modern pillars named | **the libre99 project** (the book's home: pure-Rust console + `libre99asm`/`libre99gpl` + shipped originals incl. clean-room firmware); Classic99 (Tursi/Mike Brent, incl. TI-licensed ROMs), js99er.net (Rasmus Moustgaard), MAME; F18A (Matthew Hägerty), FinalGROM 99, TIPI, nanoPEB/CF7+, SAMS; xdt99 suite | Ch. 1 §1.9 (amended v1.1) | toolchain detail in Ch. 3 |
| Provisional rulings on outline open questions | defaults adopted (see `_style.md` R-4/R-5) | session 2 | author may override |
| CPU clock (console) | 3.0 MHz | Ch. 2 §2.1 | |
| Multiplexer cost | nominal ≈ +4 cycles per 16-bit word crossing to 8-bit domain | Ch. 2 §2.2 | exact per-device figures owed by Ch. 5 |
| Console ROM | 8 K at `>0000`–`>1FFF`, 16-bit bus, zero wait | Ch. 2 §2.1/2.3 | holds vectors, ISR, FP pkg, GPL interpreter |
| Console GROMs | 3 × 6 K = 18 K (OS, menu, char sets, TI BASIC) | Ch. 2 §2.1 | |
| Scratchpad | 256 B at `>8300`–`>83FF`, 16-bit, zero wait | Ch. 2 §2.1/2.3 | (>8000–>82FF decoding story deferred to Ch. 5) |
| Expansion RAM split | 8 K low `>2000`–`>3FFF` + 24 K high `>A000`–`>FFFF`, 8-bit | Ch. 2 §2.3 | absent on bare console (holes) |
| DSR window | `>4000`–`>5FFF` | Ch. 2 §2.3 | cards page in one at a time (Ch. 30) |
| Cartridge ROM | `>6000`–`>7FFF` | Ch. 2 §2.3 | |
| MMIO asserted | sound `>8400` (w); VDP `>8800` rd data / `>8802` rd status / `>8C00` wr data / `>8C02` wr addr; speech `>9000` rd / `>9400` wr; GROM `>9800` rd data / `>9802` rd addr / `>9C00` wr data (GRAM) / `>9C02` wr addr | Ch. 2 §2.3 | ports are side-effectful (pitfall logged) |
| GPL VM state | GPL workspace `>83E0`; GPL status byte `>837C` | Ch. 2 §2.4 | detail Ch. 24/26 |
| QUIT key | `FCTN`+`=` | Ch. 2 §2.6 | etiquette in CQ-82 |
| **CQ-82** | 9-item 1982 commercial-quality checklist (v1) | Ch. 2 §2.6 | Part IX rubric; R-8 handle |
| Tower floors | Floor 1 = 9900, Floor 2 = GPL, Floor 3 = TI BASIC | Ch. 2 §2.4 | canon vocabulary (R-8) |
| Performance law | "Speed is a property of addresses, not instructions" | Ch. 2 §2.3 | proven Ch. 5, doctrine Ch. 37 |
| Sound chip identity | TMS9919 = SN94624; SN76489 family (ColecoVision/SG-1000/BBC kin) | Ch. 2 §2.1 | |
| PAL video chip | TMS9929A | Ch. 2 §2.1 | PAL policy: sidebars (R-5) |
| `JMP $` opcode | `>10FF` | Ch. 2 Lab 2 | first raw opcode asserted |
| 9918 lineage claim | 9918/28/29 → ColecoVision, SG-1000, MSX; concepts → V9938, later Sega VDPs | Ch. 2 sidebar | Guttag as source |
| Canonical build | `xas99.py -R`; outputs: default=tagged object, `-i`=EA5 image, `-b`=binary; `-L` listing | Ch. 3 §3.3 (orig.) | R-10 — **superseded by R-14** (v1.1); still the Ch. 6 interchange invocation |
| Canonical build (v1.1) | `libre99asm <src> --name '<Title>' -o build/<n>.ctg --listing … --symbols …`; `--format bin` → `<NAME>C.bin` (8 KiB-padded) | Ch. 3 §3.3/§3.4 | R-14; machine-verified session 3 |
| Artifact suffixes | `NAME`C.bin = CPU ROM cart, G = GROM, (D = 2nd bank, Ch. 35) | Ch. 3 §3.5 | community convention |
| EA5 image base | relocatable code images at `>A000` (E/A convention) | Ch. 3 §3.7 | needs 32 K |
| TI filename law | ≤10 chars, uppercase, no extensions | Ch. 3 pitfalls | FIAD truncates |
| DV80 text files | no newline bytes; records structural; cross border via xdm99/TIPI only | Ch. 3 pitfalls | detail Ch. 31 |
| ROM cart header (as synthesized & asserted) | `>6000`: `AA 01 01 00`; words: pwr-up `>0000`, program list `>6010`, DSR `>0000`, subpgm `>0000`, int-link `>0000`, spare `>0000`; entry record {next `>0000`, entry addr, name-len, name, EVEN pad} | Ch. 3 §3.7 (amended v1.1) | machine-verified session 3 (hexdump of HELLOC.bin); manual/multi-entry headers owed to Ch. 35; GPL twin Ch. 27 |
| Screen cell math | cell = 32*row+col; example `>016A` = row 11, col 10; name table `>0000` (default graphics mode) | Ch. 3 §3.7 | |
| Screen codes | machine/GPL/E-A use true ASCII; the `+>60` bias is TI BASIC's convention only | Ch. 3 §3.7 | reused Ch. 13, 28 |
| Backdrop color asserted | VR7 value `>04` = dark blue (write `>04` then `>87`) | Ch. 3 §3.7 | palette table owed to App. D |
| Entry-point convention | label `START` = libre99asm's default entry symbol (else `END` operand / `--entry`); `DEF START` belongs to the E/A loader path, taught Ch. 6 | Ch. 3 §3.7 (amended v1.1) | R-15; book-wide entry-label convention unchanged in spirit |
| COPY directive | E/A-standard include; wrapper pattern (cart header COPY main) is canon | Ch. 3 §3.7 | |
| Modern hardware roles | FinalGROM 99 = flash cart (first buy); TIPI 32K sideport or nanoPEB/CF7+ = "standard system" without PEB; F18A = VGA/enhanced VDP mod | Ch. 3 §3.6 | prices deliberately unstated (R-2) |
| v2.2 console caveat | late beige consoles' ROM-cart lockout; flash devices navigate; black-and-silver = no asterisks | Ch. 3 §3.6 | detail Ch. 35 |
| Outline deviation log | Ch. 3 "14-instruction" first program shipped as 25 instructions | Ch. 3 §3.7 | outline estimate, not contract structure |
| Lab journal | required habit from Lab 3; feeds Part IX postmortems | Ch. 3 Lab | |
| **Machine-verified (session 2)** | xas99 flags `-R -i -b -L -o` and xdm99 `-X sssd`/`-a`/`-f DF80`/`-n` confirmed against live xdt99 (GitHub head, Jul 2026); Ch. 3 HELLO + cart wrapper assemble verbatim; padded cart = 8,192 B exactly; xdm99 catalog confirms SSSD = 90 KB, 40 tracks, 9 sectors/track (pre-validates Ch. 32 §32.1); **no `--version` flag** (use `-h`) | Ch. 3 audit | strongest-evidence tier; Classic99 menu paths & FinalGROM naming still owed to tech-audit pass |
| 9900 programmer state | PC, WP, ST only; Rn = WP + 2n | Ch. 4 §4.2 | LWPI/STWP, LIMI/STST; PC via BL idiom |
| Vector map | interrupts `>0000`–`>003F` (16 levels, WP+PC pairs); XOP `>0040`–`>007F`; (LOAD vector: assert in Ch. 22) | Ch. 4 §4.3 | |
| Reserved registers | R0 shift count + never an index; R11 BL link + XOP src EA; R12 CRU base; R13–R15 = old WP/PC/ST in NEW workspace | Ch. 4 §4.2 | callee reads caller Rn at `@2n(R13)` |
| **R10 = software stack pointer** | book-wide convention (built Ch. 9; lib99 law) | Ch. 4 §4.6 | canon |
| High-byte law | byte ops on registers use the HIGH byte; constants ride high (`>4100`; `'A'*256` `[libre99asm]`); byte writes preserve low byte; `*Rn+` steps by operand size | Ch. 4 §4.4 (flag updated v1.1) | the book's #1 bug class |
| SWPB | swaps bytes, **sets no flags** | Ch. 4 §4.4 | flag-silent list owed to Ch. 8 |
| Word alignment | word ops ignore A15 (silent); EVEN after odd TEXT | Ch. 4 §4.4 | |
| ST layout | ST0 L>, ST1 A>, ST2 EQ, ST3 C, ST4 OV, ST5 OP(byte), ST6 X, ST12–15 int mask | Ch. 4 §4.5 | TI numbers from MSB |
| Flag protocols | compares grade S vs D; other ops grade result vs 0 (⇒ MOV = free test); C = no-borrow after subtract; DEC leaves C=1 until wrap | Ch. 4 §4.5 | |
| Jump grammar | H/L = unsigned, GT/LT = signed; JH=L>·~EQ, JHE=L>+EQ, JL=~L>·~EQ, JLE=~L>+EQ, JGT=A>, JLT=~A>·~EQ; **no single-op signed ≥/≤** (inversion/pair idioms); JOC/JNC/JNO/JOP | Ch. 4 §4.5 | table → App. A |
| RT | pseudo-instruction = `B *R11`, opcode `>045B` (E/A standard; xas99- and libre99asm-verified) | Ch. 4 §4.6 | |
| BLWP/RTWP contract | BLWP @vec: vec = {new WP, new PC}; old WP/PC/ST → new R13/R14/R15; RTWP inverts | Ch. 4 §4.3 | interrupts & XOP use same mechanism |
| Dest-read-before-write | 9900 reads destination before writing (visible in M counts, e.g., MOV M=4) | Ch. 4 §4.3/4.8 | MMIO implications owed to Ch. 12 |
| Timing model | T = C + 4×(accesses in 8-bit domain); MOV R,R = 14c/4a → 14/18/30 by placement; JMP = 10c/1a (matches Lab 2) | Ch. 4 §4.8 | refined by Ch. 5 measurements |
| Addressing modes | R / `*R` / `*R+` / `@sym` / `@sym(Rn)`, n≠0; both operands of Format I fully general | Ch. 4 §4.7 | costs table owed to Ch. 7/App. A |
| Jump reach | Format II displacement = signed 8-bit in words (≈ ±127 words) | Ch. 4 §4.7 | |
| 9900 peer figures | 3.0 MHz, reg-reg MOV ≈ 4.7 µs (pad); peer table values hedged "typical early clocks" | Ch. 4 §4.9 | 6502 zero-page parallel noted |
| Package facts | 64-pin ceramic DIP; +5/−5/+12 rails; external TIM9904 4-phase clock | Ch. 4 sidebar | |
| **Machine-verified (session 2, Ch. 4)** | trace.a99 (Lab 4) and all §4.3/4.4/4.6 snippets assemble via xas99: `'A'*256` literal OK, `RT` OK, `@4(R4)` OK, BSS OK; `JMP $-style HALT` emits `>10FF` (cross-checks Lab 2 ledger row); BL @HALT next-addr = HALT confirmed in listing | Ch. 4 audit | xas99-era evidence; toolchain re-founded v1.1 (see session-3 rows) |
| **The libre99 project (facts asserted)** | pure-Rust from-scratch 4A running the real firmware; embeds 137 cartridges + 15 disks; desktop app `libre99` (`cargo run --release -p libre99-app`), `--cartridge-file`/`--cartridge`/`--disk`/`--list`/`--system-grom`/`--system-rom`; F9 media browser, F5 reset, F6/F8 savestate + auto-resume, F10/F12/Tab time control, CPU inspector, screenshots, leveled log at `~/.libre99/` | Ch. 1 §1.9 / Ch. 3 §3.2 (v1.1) | source: project README + code at commit 5b1c0d0; cite README for hotkey/flag detail (R-12) |
| **Project gaps asserted** | no Speech Synthesizer emulation; disks = embedded set, DSK1 only (arbitrary `.dsk` on roadmap); GUI breakpoints/watchpoints on roadmap (CPU inspector shipped) | Ch. 1 Lab 1 / Ch. 3 §3.2 (v1.1) | re-check against README/ROADMAP each session; labs shelf these gaps |
| **BENCH99** | the book's lab bench (`code/bench/`, Rust, deps: `libre99-core` + `libre99-asm`): bare mode (CPU+bus, `pc`/`wp`/`pw`/`pb` free) & boot mode (real firmware, `boot [cart]`, `f`, `k`); `s` trace = addr/hex/disasm/cycles/ST; `u` = run-until-PC; `x` silent run; `r`/`m` side-effect-free; `screen` = name-table ASCII; `vdp` = VR0–7 | Ch. 2 Lab 2 / Ch. 3 §3.3, §3.8 (v1.1) | machine-verified session 3 (all commands exercised) |
| `.ctg` container | `ti99sim` V1 lineage: 80-byte banner (title) + region records (ROM 4 K pages / GROM 8 K pages, RLE); **banner title is emulator-side; the console menu name lives in the `>6000` header** — libre99asm sets both from `--name` | Ch. 3 §3.5 (v1.1) | verified via `cartridge.rs`/spec + built images |
| **libre99asm behaviors (machine-verified, session 3)** | R0–R15 predefined (no `-R` flag exists or is needed); entry = `START`/`END` operand/`--entry`; auto-header on by default (16-byte header + entry record + EVEN pad → HELLO entry `>6020`); `--format bin` pads to 8,192 B exactly; `--listing`/`--symbols` emitted; `COPY 'file'` single-quoted, preprocessed; **no-operand mnemonic + bare trailing comment = error (`;` required)**; `AORG` rejected in cartridge mode ("needs absolute mode"); `'A'*256`→`>4100`, `'AB'`→`>4142`, `TEXT -'…'` negates last byte, `RT`→`>045B`, `JMP $`→`>10FF` all confirmed; no macros/DEF/REF/RORG/EA5/tagged-object (Ch. 6 uses xas99 for those) | Ch. 3–4 audit (v1.1) | strongest-evidence tier; commit 5b1c0d0 |
| **HELLO end-to-end (machine-verified, session 3)** | hello.ctg (8,285 B) boots under the authentic firmware: master menu lists `2 FOR HELLO, 1981`; selection runs to `HERE` spin with WP=`>8300`, R1=`>2000`, R3=MSG+11; screen text at row 11 col 10; VDP VR7=`>04` | Ch. 3 §3.7 (v1.1) | bench transcript; also verified on bare bench (no console boot) |
| **Lab 2 measurements (session 3)** | `JMP $`: 10 cycles at `>8300`, 14 at `>A000` (+4 funnel toll, deterministic); `MOV R1,R2`: 14 (all-pad), 18 (code in expansion, WS pad) — both match the datasheet model | Ch. 2 Lab 2 / Ch. 4 §4.8 | emulator-measured = datasheet for these cases |
| **Emulator timing deviation (open)** | libre99-core executes `MOV`/`MOVB` without the 9900's destination pre-read: all-expansion `MOV R,R` measures **26**, hardware/datasheet says **30** (`A R1,R2` correctly measures 30). Body prose asserts hardware truth (R-15); bench numbers printed in Ch. 3 §3.8 (`MOVB *R3+,@>8C00`=44, `DEC`=14, `JNE`=14) are emulator-measured and inherit this caveat where a destination is re-read on real silicon | Ch. 4 §4.8 note (v1.1) | confronted in Ch. 5 §5.3 and **reconfirmed OPEN at commit 2da67ae** (`MOV R0,R1` all-slow = 26, `A R0,R1` all-slow = 30, measured); the honest probe is A-family; `cpu.rs` dest-read fix surfaced to the project (not written from the book) |
| Project originals asserted | **TITRIS** (complete falling-blocks game, `original-content/cartridges/titris/`, 1,400-line source, builds with libre99asm — Ch. 3 Ex. 3.8 reads it); **clean-room console GROM** (original title/menu + TI PYTHON REPL, boots via `--system-grom`; one documented cart incompatibility) + console-ROM rewrite track (TI BASIC deferred) | Ch. 1 §1.9 / Ch. 3 / Ch. 28 §28.7 (v1.1) | source: `original-content/system-roms/` docs at 5b1c0d0 |
| trace.a99 workspace change | Lab 4's workspace moved from in-program `BSS 32` to `WS EQU >8300` — a cartridge's bytes are ROM; workspaces must be writable (teaching point kept in the lab) | Ch. 4 Lab 4 (v1.1) | deviation from session-2 form logged |
| hello_cart.a99 retired | the manual-header COPY wrapper is replaced by libre99asm's synthesized header + the §3.7 byte-by-byte dump; hand-written headers return in Ch. 35 (`AORG` absolute mode) | Ch. 3 §3.7 (v1.1) | file removed from `code/ch03/` |
| Funnel toll (measured) | flat **+4 cycles per word access** in the 8-bit domain, identical at `>2000`/`>6000`/`>A000` (no region charges more) | Ch. 5 §5.3 | **machine-verified (bench), commit 2da67ae**; upgrades the Ch. 2 §2.2 "≈ +4 nominal" row; source `machine.rs` `wait_states` |
| Fast island (measured) | 0-wait 16-bit only in console ROM `>0000`–`>1FFF` and scratchpad `>8000`–`>83FF`; everything else +4 | Ch. 5 §5.1 | machine-verified (bench), commit 2da67ae; spot-probe `pw`/`m` confirms ROM vs RAM |
| Probe timings by placement | `JMP $` = 10 pad / 14 slow; `MOV R0,R1` = 14 / 18 / **26 core [hw 30]**; `A R0,R1` = 14 / 18 / **30** | Ch. 5 §5.3 | machine-verified (bench), commit 2da67ae (`rig.a99`); A-family is the honest probe under the open MOV deviation |
| Scratchpad mirrors (measured) | 256-byte pad images at `>8000`/`>8100`/`>8200`/`>8300`; decoder ignores A8–A9; a write through a mirror hits the live pad | Ch. 5 §5.4 | machine-verified (bench), commit 2da67ae; source `machine.rs` (`addr & 0xFF`) — matches TI Intern/Classic99 hardware truth |
| Scratchpad tenants (coarse) | `>8300`–`>831F` console low scratch; `>8320`–`>836F` claimable corner; `>837C` GPL status; `>83C0`–`>83DF` ISR/GPL work; `>83E0`–`>83FF` GPL workspace (WP) | Ch. 5 §5.2 | observed live (booted-console churn), commit 2da67ae; byte-level atlas → Ch. 24 / App. C |
| Pad-staging speedup | inner loop, 1000 laps: **28,062** cyc (cartridge) → **20,058** cyc (pad) = **1.40×** | Ch. 5 §5.5 | machine-verified (bench), commit 2da67ae (`padrun.a99`) |
| MMIO cost & read side effect | port `MOVB` 47–55 cyc vs 14 register; two reads of GROM `>9802` return different bytes (`>60` then `>01`) — the read advances the port | Ch. 5 §5.6 | machine-verified (bench), commit 2da67ae (`mmio.a99`); port vocab → Ch. 12/19/20/25 |
| 32 K detection idiom | write a pattern *and its complement* to the same cell, trust the region only if both survive; verdict in R0 (`>FFFF`/`>0000`) | Ch. 5 Field Notes | machine-verified positive case (bench), commit 2da67ae (`det32k.a99`) |
| Absent-expansion read | open bus: unowned, unreliable value (no RAM drives the lines) | Ch. 5 Field Notes | hardware truth (period refs), hedged R-2; **emulator models no unexpanded console — gap surfaced to project** |
| §5.7 memory-budget worksheet | blank form `code/ch05/budget-template.md`; every later project fills it first (R-8 canon handle) | Ch. 5 §5.7 | reusable artifact; figures drawn only from §5.1/§5.3 |
| Addressing-mode encoding | `Rn` T=00 · `*Rn` T=01 · `*Rn+` T=11 · `@LABEL` T=10 reg=0 (+1 word) · `@LABEL(Rn)` T=10 reg≠0 (+1 word); **R0 cannot index** (T=10,reg=0 is symbolic) — libre99asm rejects `@X(R0)` | Ch. 7 §7.1 | machine-verified (libre99asm error), commit 2da67ae; feeds App. A |
| Addressing-mode cycle add-ons | `*Rn` +4 · `*Rn+` +8 · `@LABEL` +8 · `@LABEL(Rn)` +8; the two `@` forms also fetch an extra instruction word (itself tolled by the code's region) | Ch. 7 §7.1 | machine-verified (bench, `modecost.a99`), commit 2da67ae; feeds App. A |
| Data-mover flag behavior | set L>/A>/EQ from the result: `MOV`, `MOVB` (also OP parity), `LI`; set **no** flags: `CLR`, `SETO`, `SWPB`, `STWP`, `STST` | Ch. 7 §7.2 | machine-verified (bench ST reads, `flagpr.a99`), commit 2da67ae |
| No byte immediate | every immediate (`LI`/`AI`/`ANDI`/`ORI`/`CI`) is word-sized; no byte-load exists | Ch. 7 §7.3 | datasheet inventory (tier 2) |
| Same-register autoincrement order | `MOV *R1+,*R1+`: source resolved+read *and incremented* before the destination is resolved; dest writes through the new R1 | Ch. 7 §7.4 | machine-verified (bench, `edges.a99`), commit 2da67ae; matches Classic99 post-increment notes |
| **libre99asm expression precedence** | **none — strictly left to right** (`2+3*4` = 20, not 14); precompute mixed add/multiply expressions | Ch. 7 §7.5 | machine-verified (libre99asm symbols), commit 2da67ae; `[libre99asm]`; relevant to Ch. 6 §6.3 |
| Pump per-word timing | `MOV *R1+,*R2+` = 30 island / 42 buffers-expansion (hw) / 46 all-expansion; copy loop (+`DEC`+`JNE`) = 50 / 62 / 74 cyc/word → **≈ 1000 / 806 / 675 words/frame**; unrolled ×2 = 40/word (1250 wpf) | Ch. 7 §7.6 / Lab | machine-verified (bench, staged in pad), commit 2da67ae; expansion-dest rows carry the MOV dest-pre-read caveat (A-family confirms hw) |
| memlib v0 costs | per unit, island / expansion(hw): `MEMCPY` 50/62 · `MEMFIL` 42/50 · `MEMCPB` 46/58 · `MEMSCN` 40/44 (no caveat — read only) | Ch. 7 Lab | machine-verified (bench), commit 2da67ae (`memlib.a99`) |
| memlib calling convention | leaf routines, `BL @NAME`/`RT` (=`B *R11`); args in R0–R2, clobbered; byte cargo rides the register's high half | Ch. 7 Lab | canon (seeds lib99, Ch. 11) |
| Border-verdict scaffold | set VDP register 7 (backdrop): write color byte then `>87` to `>8C02`; **GREEN `>02`** = pass, **RED `>06`** = fail — book-wide convention; sabotage drill required | Ch. 7 §7.7 / Lab | machine-verified (bench `vdp`), commit 2da67ae; VDP anatomy → Ch. 12; palette → App. D |
| Ch. 7 code artifacts | `code/ch07/`: memlib.a99 (+ self-test), loops.a99, hibyte.a99, setup.a99, modecost.a99, edges.a99, record.a99, flagpr.a99, membnch.a99, fldnote.a99 | Ch. 7 | all assemble via `sh verify.sh`, commit 2da67ae |
| Arithmetic flag effects | `A`/`AB`/`S`/`SB`/`AI`/`INC`/`INCT`/`DEC`/`DECT`/`NEG`/`ABS` all set L>/A>/EQ/C/OV from the result; byte forms (`AB`/`SB`, and `CB`) also set OP (parity); word ops never touch OP | Ch. 8 §8.1 | machine-verified (bench ST reads), commit 1ae787c; supersedes the "flag-silent list owed to Ch. 8" note on the Ch. 4 SWPB row |
| **INCT/DECT write C** | `INCT`/`DECT` file the full report incl. C — `INCT` on `>0010` clears a standing C (no carry), on `>FFFE`→`>0000` sets C+EQ; the §8.6 carry-chain hazard | Ch. 8 §8.1 | machine-verified (bench), commit 1ae787c |
| NEG/ABS at >8000 | `NEG`/`ABS` of `>8000` return `>8000` unchanged with OV set (the one unrepresentable magnitude); `ABS` flags reflect the ORIGINAL operand; `NEG` sets C per subtract-from-zero (C set only if operand = 0) | Ch. 8 §8.1 | machine-verified (bench), commit 1ae787c |
| Flag-silent family | alters NO status bit: `SWPB`, `CLR`, `SETO`, `STWP`, `STST`, `LWPI`, `LIMI`, `B`, `BL`, `BLWP`, `MPY`, and CRU/external (`SBO`/`SBZ`/`CKON`/`CKOF`/`RSET`/`LREX`/`IDLE`) | Ch. 8 §8.1 | bench-verified for `MPY`/`LWPI`/`SWPB`/`CLR`/`SETO`/`STWP`/`STST`; branch+CRU rows datasheet (tier 2, App. A) — extends Ch. 4 SWPB row |
| Compare operand order | `C X,Y`/`CB`/`CI` set ST as `X − Y`: L> when X higher unsigned, A> when X greater signed; non-destructive; `CB` also sets OP | Ch. 8 §8.2 | machine-verified (bench), commit 1ae787c |
| Jump pairing (compound) | unsigned `≥`/`≤` = single `JHE`/`JLE`; **signed `≥`/`≤` have NO single op** — `JGT`/`JEQ` and `JLT`/`JEQ` pairs (or invert operands); `>8000` vs `>0001`: `JH` takes, `JGT` does not | Ch. 8 §8.2 | machine-verified (bench, scraps.a99), commit 1ae787c; extends Ch. 4 jump-grammar row |
| COC/CZC | both report through EQ ALONE (leave L>/A> untouched): `COC` EQ⇔all mask bits set in dest; `CZC` EQ⇔all mask bits clear | Ch. 8 §8.2 | machine-verified (bench), commit 1ae787c |
| Shift count-0 rule | shift count field 0 → take count from R0's low 4 bits; if those are 0 → count = 16 | Ch. 8 §8.4 | machine-verified (bench: `SLA R1,0` with R0=0 → R1=0, count 16), commit 1ae787c; datasheet (App. A) |
| Shift cost | slope **2 cyc/bit**: `SLA/SRA/SRL/SRC ,n` = 12+2n island / 16+2n cart (R-14 region); R0-count path +8 cyc; SLA sets OV if sign bit changes, C = last bit out; SRA rounds toward −∞ | Ch. 8 §8.4 | machine-verified (bench: 18/24/32/46 for n=1/4/8/15 cart), commit 1ae787c; libre99-core DOES model per-count shift cost |
| MPY/DIV register-pair layout | `MPY Rs,Rd`: Rd×Rs → Rd:Rd+1 (Rd high, Rd+1 low), unsigned, flag-silent; `DIV Rs,Rd`: Rd:Rd+1 ÷ Rs → Rd quotient, Rd+1 remainder; high word in the LOWER register — book-wide 32-bit pair convention | Ch. 8 §8.5/§8.6 | machine-verified (bench: 1234×5678 → `>006A:>E9BC`; 100/10 → q `>000A` r `>0000`), commit 1ae787c |
| MPY/DIV cost | MPY = 52 island / 56 cart (flat, any operands); DIV success = 92/96 (flat on libre99-core); DIV-overflow abort = 16/20 | Ch. 8 §8.5 | machine-verified (bench), commit 1ae787c; **libre99-core models DIV cost as flat where the datasheet is data-dependent — timing-fidelity simplification surfaced to the project (not a functional error)** |
| ×10 winner | shift-and-add `(x<<3)+(x<<1)` = 76 cyc body beats MPY-form = 94 cyc body (cartridge region) — the flat MPY toll loses to a small-constant multiply; verdict flips for general 16×16 | Ch. 8 §8.5 | machine-verified (bench, mul10.a99), commit 1ae787c |
| DIV overflow rule | `DIV` refuses (OV set, dividend pair left INTACT, ~16/20-cyc bail) iff high word ≥ divisor (`≤` boundary confirmed: high = divisor also overflows); catches zero divisor | Ch. 8 §8.5 | machine-verified (bench: `>0002:>0000 ÷ >0001` and `>000A:>0000 ÷ >000A` → OV, pair intact), commit 1ae787c; DIVSAF guard idiom |
| Signed remainder convention | truncated division — **remainder takes the DIVIDEND's sign** (C's rule); book-wide for DIVS and all later signed divides | Ch. 8 §8.5 | proven by lab vectors 13–15 (`−7/2`→r`−1`; `7/−2`→r`+1`), commit 1ae787c |
| S borrow sense | subtract sets C on NO borrow, clears C on borrow (C = "no borrow"): `>0000 − >0001` → C clear; `>0005 − >0003` → C set | Ch. 8 §8.6 | machine-verified (bench), commit 1ae787c; the inverse of an add's carry for chain fix-ups |
| INCT-in-carry-chain pitfall | a pointer bump (`INCT`) between the low-word `A` and its carry jump overwrites C → high word loses the carry; buggy sum `>00000000` vs fixed `>00010000` | Ch. 8 §8.6 | machine-verified (bench, carrybug.a99), commit 1ae787c |
| Decimal converter costs | `U16DEC` (divide-by-10 chain) flat 1234 cyc (5 fixed DIVs, right-filled); `U16DEB` (subtract-table, forward, leading-zero policy) data-dependent 1524–2978 cyc for `>0000`/`>0009`/`>FFFF` (cart); digit char = value + `>30` | Ch. 8 §8.7 | machine-verified (bench), commit 1ae787c; U16DEC's flat number inherits DIV's flat-cost caveat |
| LFSR generator | `RND` = 16-bit maximal Galois LFSR, tap **`>B400`** (x^16+x^14+x^13+x^11+1); period = **65,535 PROVEN by bench count** (harness iterates seed `>0001` to recurrence, stashes `>FFFF` at `>83F8`); zero stays zero; `RNDB` ranges 0..N−1 via the high word of MPY×N; `RNDS` refuses a zero seed | Ch. 8 §8.8 | machine-verified (bench count = `>FFFF`; sabotaged tap `>A400` → period `>03FF`), commit 1ae787c |
| Console random-seed byte | word at **`>83C0`** observed to change frame-to-frame on the booted console (`0000`→`A9BE`→`08C2`); *TI Intern* maps the console seed to this scratchpad corner (tier 2, not opened this session — hedged); updated by the ISR each frame, so bare-cartridge programs see it frozen (seed from the player instead, Ch. 21) | Ch. 8 §8.8 | tier-1 observation (bench, booted console), commit 1ae787c; consistent with Ch. 5 ISR/GPL churn map (`>83C0`–`>83DF`) |
| mathtest verdict contract | pad words `>83FA` done-marker (`>C0DE` on completion), `>83FC` FAILS count, `>83FE` first-fail id, `>83F8` counted LFSR period; PASS iff `>C0DE`/`>0000`/`>0000` — the book's first self-checking lab | Ch. 8 Lab | machine-verified (29 vectors green; sabotage red id 29), commit 1ae787c |
| Ch. 8 code artifacts | `code/ch08/`: mathlib.a99 (library + START harness, the memlib single-file precedent), mathtest.bench, fields.a99, scraps.a99, mul10.a99, carrybug.a99 | Ch. 8 | all assemble via `sh verify.sh`; harness green `>C0DE/>0000/>0000`; commit 1ae787c |
| Jump reach | conditional `J**` displacement = signed byte in WORDS, −128..+127; assembler rejects out-of-range (`jump target out of range (300 bytes away; JEQ reaches -254..+256)`); **two-jump idiom** = invert the condition + `B @far` (`B` reaches all 64K) | Ch. 9 §9.1 | machine-verified (libre99asm error + bench green, `reach.a99`), commit b9291be |
| Control-flow costs | cart code / pad WS: `JMP` 14, `Jcc` 14 taken / 12 not-taken, `B @L` 24, `B *R` 16, `BL @L` 28, `RT`(=`B *R11`=>045B) 16, `BLWP @vec` 50, `RTWP` 18, `X @L`(exec `INC`) 38, dispatch `MOV @TAB(R),Rx` 34 | Ch. 9 §9.1–9.6 | machine-verified (bench single-step, `probe.a99`), commit b9291be; clean of the MOV dest-pre-read caveat (dests are registers/PC, fast island) |
| R11 link discipline | `BL` links the return address in R11; a **non-leaf must save R11 before any inner call and restore before `RT`**, else it returns into itself (before/after R11 differ); a leaf may keep R11 and `RT` directly | Ch. 9 §9.2 | machine-verified (bench: BADB4≠BADAF, guarded path green, `link.a99`), commit b9291be |
| Software stack on R10 | book-wide: R10 = stack pointer, **full-descending** (grows down, points at the top item); PUSH = `DECT R10`/`MOV x,*R10`, POP = `MOV *R10+,x`; init to a reserved pad top (demo STKTOP `>8380` growing toward `>8320`) | Ch. 9 §9.3 | machine-verified (recursion FACT(8)=40320=`>9D80`, R10 balanced to `>8380`, `stack99.a99`), commit b9291be; canon (R-16) |
| Calling convention (R-16) | args/results R0–R2 (caller-saved); leaf keeps R11+`RT`; non-leaf PUSHes R11 (+ any preserved reg), POPs in reverse; R10, R13–R15 never scratch; **errors in R0** (>0000 success, nonzero code; test `MOV R0,R0`/`JNE`) — generalizes DIVSAF | Ch. 9 §9.3/§9.9 | canon (R-16); seeds `lib99` (Ch. 11) |
| BLWP/RTWP linkage | `BLWP @vec`: vector = 2 words `{new WP, new PC}`, placeable ANYWHERE in RAM; loads WP+PC, saves old WP/PC/ST → new R13/R14/R15. `RTWP`: WP←R13, PC←R14, ST←R15 | Ch. 9 §9.4 | machine-verified (bench: service read R13/R14/R15 = `>8300`/`>6040`/`>C000`, `probe.a99`), commit b9291be; extends the Ch. 4 vector row |
| Jump-table dispatch | `MOV @TAB(Rn),Rx` then `B *Rx`; selector × 2 for the word offset (entry 1 → offset 2); constant-time n-way switch | Ch. 9 §9.5 | machine-verified (bench: selector 1 → case D1 → `>00D1`, `probe.a99`), commit b9291be |
| X instruction | executes the instruction WORD at its operand in place, then continues after the `X`; uses = dispatch-by-instruction, run-time-selected op, patched-then-X | Ch. 9 §9.6 | machine-verified (bench: `X @XTGT` runs `INC R5` → R5=1, 38 cyc, `probe.a99`), commit b9291be |
| XOP trap | `XOP src,n` traps through the vector at **`>0040` + n×4** (console ROM — you CANNOT install your own on a stock 4A); saves old WP/PC/ST → new R13/R14/R15 like BLWP, PLUS new **R11 = operand EFFECTIVE ADDRESS**, PLUS sets the ST **X bit** (bit 6) | Ch. 9 §9.7 | machine-verified (bench single-step: `XOP @OPND,0` → PC `>0C1C`, WP `>280A`, R11=&OPND, ST X set; XOP-0 vector reads `{>280A,>0C1C}`, `xop.a99`), commit b9291be |
| Console vectors (read cold) | reset **`>0000`** = `{WP >83E0, PC >0024}`; level-1 (VDP) interrupt **`>0004`** = `{WP >83C0, PC >0900}` (ISR workspace `>83C0` = the Ch. 8 seed-byte corner); interrupt vectors (levels 1–15) fill `>0004`–`>003F`; XOP vectors fill `>0040`–`>007F` | Ch. 9 Field Notes | machine-verified (bench `m 0000`, booted console), commit b9291be; consistent with the Ch. 8 `>83C0` ISR churn |
| task99 multitasker | cooperative 2-coroutine scheduler: each task owns a workspace; yield = `BLWP @YVEC` to a service that saves the caller's ctx (R13/R14/R15) into a 3-word control block `{WP,PC,ST}`, swaps CUR/NXT pointers, loads the other block, `RTWP`; shared log interleaves `AA BB AA BB AA BB AA BB` (fair switch proven); 216 B whole file / 160 B multitasker / ~50 B scheduler core; 218 instr, 5556 cyc | Ch. 9 Lab | machine-verified (bench green VDP R7=`>02`, `task99.a99`), commit b9291be |
| Ch. 9 code artifacts | `code/ch09/`: reach.a99 (two-jump idiom), link.a99 (R11 discipline), stack99.a99 (R10 stack + recursion), probe.a99 (control-flow costs + BLWP/X/dispatch), task99.a99 (cooperative multitasker), xop.a99 (XOP transfer) | Ch. 9 | all assemble via `sh verify.sh`; verdict files green; commit b9291be |
| CRU space | Communications Register Unit = a 4096-bit space of single-bit I/O, entirely SEPARATE from memory; five instructions (SBO/SBZ/TB single-bit, LDCR/STCR multi-bit); every non-memory-mapped device (keyboard, joysticks, cassette, interrupt mask, expansion cards) lives here | Ch. 10 §10.1 | datasheet (tier 2, App. A); 990 industrial-control heritage; extends the Ch. 2 "three fabrics" row |
| R12 CRU base + ×2 | R12 holds the CRU base; the bit address sits in R12 **bits 3–14**, so the value in R12 = **2× the hardware bit number** (bit >0012 ↔ R12 >0024; bit >0880 ↔ R12 >1100); single-bit ops add a signed *bit* displacement to the base | Ch. 10 §10.2 | machine-verified (bench: `SBO 0` @R12=>1100 hits disk-card bit >0880; keyscan drives R12 >0024 = bit 18), commit 84c585a; extends the Ch. 4 R12 row |
| SBO/SBZ/TB | `SBO` sets a CRU bit to 1, `SBZ` to 0 (both OUTPUTS, flag-silent); `TB` tests a bit into EQ (the INPUT, the only one touching ST); **12 cyc each** | Ch. 10 §10.3 | machine-verified (bench, crudemo.a99), commit 84c585a; SBO/SBZ flag-silent confirmed Ch. 8 |
| CRU read ≠ write | on the 9901 a CRU bit's read and write are often DIFFERENT signals (write drives an output pin, read senses an input pin) — a round trip (write then `TB`) works only on genuinely latched bits (disk card >1100 bits 0/7), NOT the 9901's own outputs (alpha-lock, etc., which read idle-high) | Ch. 10 §10.3 | machine-verified (bench: crudemo round-trips the >1100 latches; `SBO 21`/`TB 21` do not correspond), commit 84c585a |
| LDCR/STCR | move `count` bits (1–15; **0 = 16**) between an operand and the CRU from the base; **≤8 bits → BYTE operand** (data in the high half, high-byte law), ≥9 → word; bits transfer **LSB-first** (base ↔ low bit) | Ch. 10 §10.4 | machine-verified (bench: `STCR R2,8`→>0081, `STCR R4,12`→>0FFF, `LDCR >0081,8` sets bits 0+7; costs LDCR 20+2·count, STCR 42/44/58/60), commit 84c585a |
| Keyboard scan (9901) | matrix scanned via CRU: column select = `LDCR Rx,3` at R12=>0024 (CRU bits 18–20); row read = `STCR Rx,8` at R12=>0006 (CRU bits 3–10), **active-low** (pressed = 0), **LSB-first** (row r → bit r); (column,row) indexes an ASCII table | Ch. 10 §10.5 | machine-verified (bench, keyscan.a99: 12 keys A→>41 … Q→>51; decode proven via injected 9901 row byte — see the bench-gap row), commit 84c585a; ROM KSCAN = Ch. 23 |
| DSR CRU convention | each expansion card claims a CRU base (disk >1100, RS-232 >1300, … App. G); **bit 0 of the base pages the card's DSR ROM into the >4000 window** (`SBO 0` in, `SBZ 0` out); a valid DSR shows the **>AA** signature at >4000 | Ch. 10 §10.6 | machine-verified (bench, cruscan.a99: disk-card CRU decode answers at >1100), commit 84c585a; DSRs = Ch. 30 |
| CRU bench gaps (R-12) | (1) no keypress-into-user-code path — BENCH99 `k` works only in BOOT mode, `pc`/`wp` only in BARE mode (mutually exclusive); keyscan decode verified via injected 9901 row bytes + the core's own CRU test; (2) no DSR ROM installable from the bench (>4000 reads open bus at every base) — use the desktop emulator/Classic99 for live cards; (3) 9901 cassette-relay/motor output bits unemulated (deferred, project ROADMAP §6) — the CRU logic runs but no relay closes | Ch. 10 | surfaced to project (bench observation), commit 84c585a; possible roadmap items |
| Ch. 10 code artifacts | `code/ch10/`: keyscan.a99 (9901 keyboard scan + decode), crudemo.a99 (SBO/SBZ/TB + LDCR/STCR self-test on the disk-card latches), cruscan.a99 (DSR CRU-base scan) | Ch. 10 | all assemble via `sh verify.sh`; verdict files green; commit 84c585a |
| libre99asm COPY convention | `COPY 'file'` pulls a shared source file in at assembly; **libre99asm resolves the path relative to the SOURCE FILE's directory**, NOT the working dir (a sibling include → bare filename `COPY 'equates.inc'`; `COPY 'code/ch11/equates.inc'` from a source already in `code/ch11/` fails); an EQU-only include takes a non-`.a99` extension (`.inc`) so verify.sh's `*.a99` sweep skips it; programs compose by COPY, not a linker (no DEF/REF) | Ch. 11 §11.3 | machine-verified (bench: syschk.a99 assembles with `COPY 'equates.inc'`+`COPY 'assert.inc'`), commit 84c585a; `[libre99asm]` |
| lib99 conventions (R-17) | the book's accumulating library (`code/lib99/`; README = contract): each module = one file, R-16 convention, header + per-routine register-use map, addresses via `equates.inc`, a `START` self-test in the mathlib single-file pattern + a `.bench` verdict script; versioned by the book (grows one chapter at a time); founding modules `memlib` (Ch. 7), `mathlib` (Ch. 8) | Ch. 11 Lab | canon (R-17); born Ch. 11 |
| SYSCHK diagnostic | lib99's first consumer: RAMPRB (det32k pattern → RAM present?), VSADDR/VDPCHK (VDP responds + VRAM size by counter-wrap walk = **16 KiB, wrap >4000** — first tier-1 confirmation of the canon-card VRAM figure), DSRSCN (CRU DSR-base scan for >AA). Findings block at `>8320`: DONE `>C0DE` / FLOWR / FHIGH / F32K / FVDP / FVSIZE(`>4000`) / FDSRC / FDSRM / FVERD; **verdict gates ONLY on VDP-present-and-16K** (RAM/DSR findings REPORTED, not gated — honest values are rig-dependent) | Ch. 11 Lab | machine-verified (bench: `>8320`=>C0DE, both RAM banks `>FFFF`, FVSIZE `>4000`, 0 DSR, R7=`>02` green; sabotage 8K→FVERD `>0001`+red), commit 84c585a; matches vdp.rs `[u8; 0x4000]` |
| VERDCT idiom | `assert.inc`: a leaf that stamps a verdict/panic code to a caller-named pad word AND paints the border (R0=`>0000`→green, nonzero→red) in one call — the macro-free assertion + panic-dump (§11.5); caller names the pad word, so it dictates no layout | Ch. 11 §11.5 | machine-verified (bench: SYSCHK green path + sabotage red path), commit 84c585a |
| SYSCHK bench gaps (R-12) | (1) the project emulator always backs `>2000`/`>A000` with real RAM — **no unexpanded-console model**, so RAMPRB's negative case is unreachable on the bench (reconfirms the Ch. 5 gap); (2) **no DSR ROM installable from BENCH99's bare bus**, so DSRSCN finds 0 signatures (the `>1100` disk-card CRU decode still answers) | Ch. 11 Lab | surfaced to project (bench observation), commit 84c585a; possible roadmap items (overlaps the Ch. 10 CRU-bench-gaps row) |
| Ch. 11 code artifacts | `code/ch11/`: equates.inc (named-addresses include), assert.inc (VERDCT idiom), syschk.a99 (SYSCHK library + harness), syschk.bench (CI script); `code/lib99/README.md` (library contract) | Ch. 11 | all assemble via `sh verify.sh` (`.inc` files skipped by the `*.a99` sweep, COPY'd into syschk.a99); SYSCHK green; commit 84c585a |
| VDP four ports | `>8800` read VRAM data (returns a PREFETCHED byte, then auto-increments); `>8802` read status (side effect: clears F/5S/C **and** resets the address flip-flop); `>8C00` write VRAM data (auto-increments); `>8C02` write address counter / register (two-byte control sequence) | Ch. 12 §12.2 | machine-verified (bench vdplib + `vram` oracle), commit e97e8ce; matches vdp.rs |
| VDP two-byte address protocol | aim the 14-bit counter via `>8C02`: **low 8 bits first**, then a second byte — `00aaaaaa` = READ-setup (prefetches), `01aaaaaa` = WRITE-setup, `100000rrr` (top bit) = write register rrr with the first byte; a flip-flop tracks first/second byte (reset by a status read); the counter auto-increments (mod 16 KiB) after each data access — aim once, then stream | Ch. 12 §12.2 | machine-verified (bench, vdplib VWA/VRA), commit e97e8ce |
| VDP registers R0–R7 | WRITE-ONLY chip config (keep a RAM shadow): R0 = M3(bit1)/EXT; R1 = 16K/BL(blank↔enable)/IE(int-enable)/M1/M2/SZ(sprite size)/MG(magnify); table bases — R2 name `(R2&>0F)<<10`, R3 color `R3<<6`, R4 pattern `(R4&>07)<<11`, R5 sprite-attr `(R5&>7F)<<7`, R6 sprite-pat `(R6&>07)<<11`; R7 = fg/text(hi nibble) \| backdrop(lo nibble, = the border-verdict color) | Ch. 12 §12.3 | datasheet + vdp.rs; base formulas machine-verified (bench, vdplib VWTR), commit e97e8ce |
| VDP status byte | read `>8802`: bit7 **F** (frame/vblank; requests level-1 interrupt when R1 IE set), bit6 **5S** (fifth sprite), bit5 **C** (coincidence), low 5 = fifth-sprite number; **reading clears F/5S/C** (the interrupt acknowledgement) and resets the address flip-flop → exactly one owner reads status per frame (Ch. 17) | Ch. 12 §12.4 | vdp.rs (`read_status` `&= 0x1F`, matches Classic99 Tiemul.cpp); F-heartbeat detailed Ch. 17 |
| Table-driven video model | the picture is three tables chained by indirection: **name** (1 byte/cell → pattern number), **pattern** (8 bytes → 8×8 shape, 1 bit/pixel), **color** (fg/bg); the chip does name→pattern→color per cell per frame. NO framebuffer. Every Part III mode = a different table layout | Ch. 12 §12.5 | canon (the model for chs 13–16); vdp.rs rasterizers |
| VDP port timing | `MOVB` reg→`>8C02` = **34 cyc**; full counter aim (VWA) ≈ **134 cyc**; `MOVB *src+→>8C00` = **40 cyc** (source in fast island) / **44** (slow source); bulk VMBW loop ≈ **68–72 cyc/byte**; name-table clear (768 B) ≈ 52,000 cyc ≈ **1 frame**; a hypothetical 49,152-B framebuffer ≈ **40 frames** to fill | Ch. 12 §12.6 | machine-verified (bench single-step, vdplib), commit e97e8ce; **VDP-port MOVB costs inherit the MOV dest-pre-read deviation (hardware ~+4/port write)** |
| bench `vram` command | BENCH99 gains `vram <hex> [n]` — dumps the VDP's private 16 KiB VRAM in the `m`/cmd_mem hex format; an **independent** graphics oracle (the CPU can't address VRAM directly); read-only diagnostic | Ch. 12 tooling | added commit e97e8ce (`code/bench/src/main.rs`) |
| Ch. 12 code artifacts | `code/ch12/`: vdplib.a99 (VDP core library `VWTR/VWA/VRA/VSBW/VMBW/VSBR/VMBR` + self-test; refactored to vdplib.inc + harness in Ch. 13), vram-budget-template.md (§12.7 worksheet) | Ch. 12 | vdplib assembles via `sh verify.sh`; self-test green (R7=>02), pattern round-trips VRAM (`vram 0100` = `01 23 45 67 89 AB CD EF`), VSBW/VSBR byte `>5A`; commit e97e8ce |
| Graphics I structure | three tables: **name** (768 B = 32×24, one pattern# per cell), **pattern** (256 × 8-byte 8×8 glyphs = the font/tileset), **color** (32 B, one fg/bg pair per group of 8 pattern numbers); mode registers R0=>00, R1=>E0 (16K + display enable + Graphics I), R2=>00 name, R3=>0E color (>0380), R4=>01 pattern (>0800), R7 backdrop | Ch. 13 §13.1 | machine-verified (bench, textlib TXMODE: R1=>E0/R3=>0E/R4=>01), commit 31417ef; vdp.rs render_graphics1 |
| Eight-character color rule | in Graphics I a character's color = color-table[pattern# ÷ 8] — two characters share a color IFF their pattern numbers are in the same group of eight (regardless of screen position). With textlib's ASCII=pattern# convention, chars sharing ASCII÷8 share color ("HELLO" spans groups 8–9 = ASCII >40–4F, so cannot be recolored alone). Design rule: give same-colored things pattern#s in one group | Ch. 13 §13.1/§13.5 | the constraint defining the TI look; vdp.rs (`color = vram[col + ch>>3]`) |
| lib99 composition (.inc + harness) | a module USED by another module = a routines-only `<name>.inc` (COPY-includable) + a `<name>.a99` self-test harness (COPYs deps in order + the .inc + START); verify.sh assembles the .a99 and skips the .inc; consumers COPY dependencies in DEPENDENCY ORDER (flat namespace, no linker). `vdplib` refactored to `vdplib.inc` + `vdplib.a99` here | Ch. 13 §13.3 | machine-verified (bench: refactored vdplib still green; textlib.inc composes on vdplib.inc), commit 31417ef; extends R-17; `code/lib99/README.md` updated |
| textlib module (lib99) | `code/ch13/textlib.inc`: TXMODE (Graphics I + font + colors + clear), TXCLS, TXLOC (R0=row,R1=col), TXPUTC (R1hi=char; `>0D`=CR; wrap at 768), TXPUTS (R1=→null-terminated string), TXHEX (R1=value → four hex digits); state = CURPOS (`>8342`, linear cell 0–767); **name-table byte = the character's ASCII code** (so `screen` shows the text, and pattern[ASCII] is the glyph); ships a hex-digit font | Ch. 13 §13.3/§13.4 | machine-verified (bench: self-test green R7=>02, `screen` shows C0DE/DEAD, font at `vram 0980`/`0A08`), commit 31417ef |
| textlib gotchas | (1) TXMODE must write the mode/enable byte `>E0` to register **1**, not 0 — a reg-0 slip leaves the display BLANKED (invisible to `screen`, caught by `vdp` showing R1=>00); (2) VSBR returns the byte in R1's **HIGH** half (low half unspecified) → compare with `CB`, not `CI` | Ch. 13 §13.3 | machine-verified (both bugs found + fixed in verification), commit 31417ef |
| Status-line convention | the book reserves screen **row 23** (bottom) as a status line from Ch. 13 on — a one-row panel where running programs report mode/file/error/prompt | Ch. 13 §13.6 | canon (book-wide convention) |
| MONITOR99 | Lab 13: a live hex memory viewer built on textlib — eight rows of "ADDR WWWW WWWW WWWW WWWW" via TXLOC/TXHEX/TXPUTC; its dump of `>0000` matches memory byte-for-byte (row 0 = `83E0 0024 83C0 0900` = the Ch. 9 vectors, on the TI's own screen); ~283,000 cyc (~6 frames) full setup+dump, dominated by the one-time 768-cell clear + font load | Ch. 13 Lab | machine-verified (bench `screen` vs `m 0000`), commit 31417ef |
| Ch. 13 code artifacts | `code/ch13/`: textlib.inc (text engine), textlib.a99 (self-test harness), monitor99.a99 (Lab); `code/ch12/`: vdplib.inc + refactored vdplib.a99 | Ch. 13 | all assemble via `sh verify.sh` (34 sources); textlib + MONITOR99 green; commit 31417ef |
| VDP mode-select bits | the four display modes select on M1/M2/M3 = R1 bit4 / R1 bit3 / R0 bit1: Graphics I `000`, Graphics II/bitmap `001`, Multicolor `010`, Text `100`; only one bit legal at a time (two = undefined, our emulator falls back to Graphics-I-like) | Ch. 14 Bridge/§14.1 | datasheet + vdp.rs `mode()` (m1=reg1&0x10, m2=reg1&0x08, m3=reg0&0x02); machine-verified (bench, carousel R1 marches >E0→>D0→>C8), commit 0d3e5d5 |
| Text mode structure | 40×24 name table (960 entries); **6-pixel cells** — the chip draws only the top 6 bits of each pattern byte, low 2 discarded (40×6=240 px inside the 256-px line); NO colour table and NO sprites; R7 = foreground (high nibble) \| background+border (low nibble) for the whole screen. Setup: R1=>D0 (16K + display-enable + M1), R0=>00, R2=>00 name (>0000), R4=>01 pattern (>0800); R3/R5/R6 unused | Ch. 14 §14.1 | machine-verified (bench, textlib40 TX40MD: R1=>D0, R7=>F4; `screen` 40-wide; `pixels 1` shows the glyphs), commit 0d3e5d5; vdp.rs render_text (40 cols, `px in 0..6`) |
| 6-pixel-portable font | a glyph drawn inside the top FIVE bits of each byte (bits 7–3, low three clear) renders identically as an 8-wide Graphics I cell and a 6-wide text cell — text mode's low-2-bit truncation changes nothing — so ONE font serves both modes; textlib40 ships a full A–Z + 0–9 font on this rule (reuses ch13's digit and A–F glyphs, extends G–Z) | Ch. 14 §14.1/§14.2 | machine-verified (bench `pixels 1` reads the alphabet back glyph-correct, incl. M/Q/V/W/X/Z), commit 0d3e5d5 |
| textlib40 module (lib99) | `code/ch14/textlib40.inc`: TX40MD (text mode + font + colours + clear), TX40CL, TX40LC (row R0, col R1 → CUR40=row*40+col), TX40PC (R1hi=char; >0D=CR; wrap at 960), TX40PS (null-terminated string), TX40HX (value → 4 hex digits); cursor CUR40=>8346; TXCOLR=>F4 (white on dark blue). Positioning uses row*40=(row<<5)+(row<<3) (40 ≠ 2^n → no MPY); CR uses `DIV` by 40 (no bitmask finds a row start) | Ch. 14 §14.2 | machine-verified (bench: self-test green R7=>F2, `screen` shows alphabet+pangram "PACK MY BOX WITH FIVE DOZEN LIQUOR JUGS"+"HEX C0DE READS C0DE"), commit 0d3e5d5; foundation for Ch. 42 editor |
| Multicolor mode structure | 64×48 grid of fat 4×4 pixels, each a colour 0–15 or transparent. Same 32×24=768-entry name table as Graphics I, but definition bytes read as COLOUR PAIRS (hi nibble = left 4×4 block, lo = right); within each group of 4 char rows, rows read def bytes 0–1/2–3/4–5/6–7. Setup: R1=>C8 (16K + enable + M2), R0=>00, R2=>02 name (>0800), R4=>00 canvas/pattern (>0000), R7 backdrop | Ch. 14 §14.3 | machine-verified (bench, mcolib MCMODE: R1=>C8, R2=>02, R4=>00; `pixels 4` bars), commit 0d3e5d5; vdp.rs render_multicolor |
| Multicolor name-table map | fill the 768 name entries with `name[i] = ((i>>7)<<5) \| (i&31)` (i=0..767) so each group of 4 char rows names a distinct run of definitions and the 768 entries spread over 192 defs × 8 bytes = a flat **1536-byte canvas** | Ch. 14 §14.3 | machine-verified (bench, MCMODE fill + `pixels 4` bars land correctly), commit 0d3e5d5 |
| PLOT64 addressing | fat pixel (x 0–63, y 0–47) lives in canvas byte `(y>>3)*256 + (x>>1)*8 + (y&7)`, in the HIGH nibble if x even (left block) / LOW nibble if x odd; plotting = read-modify-write that one nibble (mcolib MCPLOT). **Colour 0 = transparent everywhere** (shows the backdrop — a hole, not a colour; palette renders it identically to colour 1 black) | Ch. 14 §14.3 | machine-verified (bench: 16 colour bars read `2233…ffff` via `pixels 4`, canvas[>F8]=>FF self-check), commit 0d3e5d5 |
| mcolib module (lib99) | `code/ch14/mcolib.inc`: MCMODE (multicolor + name map + clear), MCCLS (flood the canvas with one colour), MCPLOT (R0=x, R1=y, R2=colour → set one fat pixel); CANVAS=>0000, MCNAME=>0800 | Ch. 14 §14.3/Lab | machine-verified (bench: colour-bars self-test green R7=>02), commit 0d3e5d5 |
| Clean mode-switch discipline | changing modes at runtime = four steps: (1) BLANK (clear R1 bit6 display-enable), (2) rewrite mode + table-base registers, (3) re-lay-out the new mode's VRAM, (4) UNBLANK. Blank first so the beam never draws a half-built screen. Hazards: forgetting to blank (garbage flash, invisible under single-step), forgetting to rebuild tables, and clobbering the 16 KiB bit (R1 bit7 — why BLANK writes >80 not >00). Shared tables at the same addresses (name >0000, pattern >0800) make a Graphics I↔text switch move no font bytes | Ch. 14 §14.4 | machine-verified (bench, carousel BLANK + 3-phase switch, each mode built correctly), commit 0d3e5d5 |
| bench `pixels` command | BENCH99 gains `pixels [step]` — renders the real 256×192 frame via the VDP rasterizer and prints one palette-index hex digit per sampled pixel (default step 4 → 64×48, one glyph per multicolor fat pixel); the **pixel-level oracle** for Part III (proves the bytes actually paint the picture, not just that they landed). Indices 0/1 both black → both read `0`. Also: `screen` is now mode-aware (40 columns in text mode, 32 otherwise) | Ch. 14 tooling | added commit 0d3e5d5 (`code/bench/src/main.rs`); uses only libre99-core public API (Vdp::render, WIDTH/HEIGHT/PALETTE) |
| Ch. 14 code artifacts | `code/ch14/`: textlib40.inc + textlib40.a99 (40-col text engine + 6px font), mcolib.inc + mcolib.a99 (PLOT64 multicolor canvas), carousel.a99 (3-mode switch demo), monitor40.a99 (MONITOR99 at 40 cols) | Ch. 14 | all assemble via `sh verify.sh` (38 sources); textlib40/mcolib/carousel/monitor40 verified; monitor40 dump matches `m 0000`; commit 0d3e5d5 |
| Graphics II (bitmap) structure | M3=1; a bit per pixel over 256x192. Screen in three horizontal THIRDS, each with its own 2 KiB pattern + 2 KiB colour region; 768-entry name table filled 0..255 three times unrolls the "characters" into a linear 6 KiB pixel canvas + parallel 6 KiB colour table. **8x1 colour-pair rule**: one fg/bg pair per horizontal run of 8 pixels (the attribute clash) | Ch. 15 §15.1 | machine-verified (bench, bmplib box/X + grapher), commit 0d3e5d5; vdp.rs render_graphics2 |
| Bitmap register setup + mask bits | R0=>02 (M3), R1=>E0 (16K+enable), R2=>06 name (>1800), R3=>FF colour (>2000), R4=>03 pattern (>0000). R3/R4 are select-bit + address-MASK, not plain bases: **R4=>03** low 2 bits open the full pattern span — R4=>00 masks it to 2 KiB that REPEATS in all three thirds (the classic bitmap bug); **R3=>FF** = bit7 (colour at >2000) + >7F full mask — R3=>7F drops colour onto the pattern at >0000 | Ch. 15 §15.2 | machine-verified (bench, bmplib BMODE regs), commit 0d3e5d5; mask logic from vdp.rs render_graphics2 (pat_mask/color_mask) |
| Bitmap pixel addressing | pixel (x 0-255, y 0-191) -> pattern byte `(y AND >F8)*32 + (x AND >F8) + (y AND 7)`, bit `>80 >> (x AND 7)`; the colour twin is the same offset in the >2000 table. Plot = read-modify-write (VSBR/SOCB/VSBW) so neighbours in the byte survive; bit mask from an 8-entry lookup (variable shift count needs R0, already busy) | Ch. 15 §15.3 | machine-verified (bench: box corners at vram >17FF=>FF, >0CF8=>01; edges exact), commit 0d3e5d5 |
| bmplib module (lib99) | `code/ch15/bmplib.inc`: BMODE (Graphics II + linear name map + clear), BCLS (clear pixels, set canvas colour BCOLR), BPSET (turn on pixel x,y), BLINE (Bresenham, integer-only, signed JLT/JGT — the error term goes negative); BPATT=>0000, BCOLT=>2000, BNAME=>1800; BMASK bit table. Monochrome (one canvas colour); per-8x1 clash is the colour exercises' playground | Ch. 15 §15.3/§15.4/Lab | machine-verified (bench: box+X self-test green R7=>02, all 4 edges + both diagonals; grapher parabola), commit 0d3e5d5 |
| Bitmap cost ledger | pattern 6 KiB (>0000-17FF) + colour 6 KiB (>2000-37FF) + name 768 B (>1800-1AFF) = **~12.75 of 16 KiB** committed; ~3 KiB left, NO room for a second screen (bitmap can't double-buffer like Graphics I). Hybrid/"half-bitmap" layouts reclaim unused thirds' tables | Ch. 15 §15.7 | machine-verified layout (bench, bmplib), commit 0d3e5d5 |
| Bitmap clear cost | bringing up + clearing the bitmap (768 name bytes + 12,288 pattern+colour bytes through the port) = **869,894 cycles ≈ 17 frames** (@50k cyc/frame). Full-screen bitmap animation at 60 Hz is IMPOSSIBLE; the only strategy is to touch few pixels (dirty-rectangle) — sets up Ch. 17's game loop | Ch. 15 §15.8 | machine-verified (bench `u` + `cycles`, BMODE to return), commit 0d3e5d5; ~66 cyc/byte, consistent with Ch. 12's ~68-72 |
| Ch. 15 code artifacts | `code/ch15/`: bmplib.inc + bmplib.a99 (bitmap engine + Bresenham), grapher.a99 (function-plot demo) | Ch. 15 | both assemble via `sh verify.sh` (40 sources); box+X + parabola verified via `pixels`/`vram`; commit 0d3e5d5 |
| Sprite hardware model | 32 sprites, hardware-composited over any non-text background. Sprite Attribute List (SAL) at >0300 (R5=>06): 32 entries x {Y, X, pattern#, early-clock|colour}. Y = the line ABOVE (sprite shows at Y+1); Y=>D0 ENDS the active list; early-clock bit (byte3 bit7) shifts 32 px LEFT (smooth left-edge entry); colour = byte3 low nibble (one colour/sprite + transparent). Size 8x8/16x16 = R1 bit1 (SZ), 2x magnify = R1 bit0 (MG), both GLOBAL. Sprite patterns at >1000 (R6=>02), 8 bytes/8x8 (16x16 = 32 bytes, pattern# steps 4) | Ch. 16 §16.1/§16.2 | machine-verified (bench, spritelib SAL dump >0300 = 5F 18 00 0F...), commit 0d3e5d5; vdp.rs scan_sprites |
| Law of four | at most FOUR sprites draw per scanline; a 5th sets the fifth-sprite flag (status bit6) + records its number (status low 5 bits) and is NOT drawn; lower-numbered sprites win. Verified: 5 balls on Y=96 -> pixels shows 4 (sprites 0-3), sprite 4 (x=216) absent. **Flicker multiplexing**: rotate which 4 the hardware draws each frame (rotate SAL order) to fake more, at the cost of shimmer (persistence of vision) | Ch. 16 §16.3 | machine-verified visually (bench pixels, 5th sprite dropped), commit 0d3e5d5; status-flag read is program-side (bare bench has no non-destructive status peek) |
| Sprite motion + auto-motion | manual: SMOV rewrites Y,X. Smooth sub-pixel = **8.8 fixed-point** (high byte = whole pixel, low = 256ths); add 8.8 velocity/frame, hand only the high byte to the hardware. Console firmware offers FREE automatic constant-velocity motion via a VDP motion table (std >0780) serviced by the frame ISR (Ch. 22) — convenient for dumb drift, but constant-velocity only; serious games bypass it and move by hand for anything that accelerates/bounces/steers | Ch. 16 §16.4 | 8.8 = Ch. 8/36 fixed-point; auto-motion described from firmware docs (bare bench doesn't run the ISR); Ch. 22 drives it |
| Sprite collision | hardware = ONE bit, the coincidence flag (status bit5): SOME two sprites' opaque pixels overlapped — not which, not where. Games compute **bounding boxes** instead: 8x8 sprites collide iff |dx|<8 AND |dy|<8 (two S/ABS/CI). Hybrid: coincidence flag as a cheap "anything?" early-out, box tests only when set. Lower sprite# = priority; colour 0 = transparent (shapes each sprite, lets sprites layer) | Ch. 16 §16.5/§16.6 | machine-verified (bench, DODGE box test fires: DHITS=1 when meteor 0 falls onto the player), commit 0d3e5d5 |
| spritelib module (lib99) | `code/ch16/spritelib.inc`: SINIT (R5=>06/R6=>02, empty list), SPAT (load 8x8 shape #R0 from R1), SPUT (place sprite R0 at screen R1=y/R2=x, shape R3, colour R4 — writes y-1, streams 4 SAL bytes), SMOV (move y,x only), SHIDE (park at Y=>C0). SATTR=>0300, SPATT=>1000 | Ch. 16 §16.1/Lab | machine-verified (bench: self-test law-of-four green R7=>02), commit 0d3e5d5 |
| DODGE (first game engine) | Lab 16: player ball + 4 falling meteors; per-tick TICK = fall (y+=6) / wrap+respawn at LFSR column when y>=184 / bounding-box collide with player -> DHITS++ + respawn; SMOV redraws each. Deterministic bench proof: meteor 0 starts over the stationary player -> collides (DHITS=1). Interactive input = Ch. 21, frame-timed loop = Ch. 17, sound = Ch. 19 | Ch. 16 Lab | machine-verified (bench: DHITS=1, meteors fell to y=174, sprites render via pixels), commit 0d3e5d5 |
| Ch. 16 code artifacts | `code/ch16/`: spritelib.inc + spritelib.a99 (sprite engine, law of four), dodge.a99 (game engine) | Ch. 16 | both assemble via `sh verify.sh` (42 sources); spritelib + DODGE verified; commit 0d3e5d5 |
| Frame budget (60 Hz) | one video frame = **~50,000 cycles** at 3 MHz (measured: 50,014 for 1 frame, 500,117 for 10 = 50,011.7 avg). At ~40 cyc/VDP-byte-write that's ~1,200 VDP writes for EVERYTHING a frame does. The VDP F flag (status bit7) marks the beat; reading status clears it (the once-per-frame ack) | Ch. 17 §17.1 | machine-verified (bench boot mode: cycles before/after `f 1`/`f 10`), commit 0d3e5d5 (re-confirmed vs bd1bbb6) |
| No hardware scroll (9918A) | the 9918A has NO scroll register (unlike the 9938 and later chips) — to scroll the background you move the bytes yourself. Full-screen coarse scroll = rewrite all 768 name cells = **~163,000 cycles ≈ 3.3 frames** (measured), so full-screen coarse scrolling DOES NOT fit a frame. Escapes: scroll a band only (32 cells), scroll less often, or the pattern-shift method | Ch. 17 §17.4 | machine-verified (bench, terrain.a99 one-tick delta ~163k cyc; stripes scroll left 1 col/tick via pixels), commit 0d3e5d5 |
| Pattern-shift smooth scroll (Parsec method) | leave the name table alone; a band's cells all name a few "scroll" characters, and each frame you redraw THOSE characters' patterns shifted one pixel (feeding the map's next column at the edge) — the band slides 1 px/frame, one coarse band-shift every 8th. Cost = **~1,044 cycles** for one shift (measured) vs ~163,000 for a full rewrite — **~150x cheaper**, inside the 50k frame. Pays for the # of tiles, not the # of cells | Ch. 17 §17.5 | machine-verified (bench, smooth.a99: pattern byte walks >80→>40→>20, one-shift delta ~1,044 cyc), commit 0d3e5d5 |
| Game-loop shapes + time | two loops: **poll-synchronized** (interrupts off, do work then spin reading status until F — full control, no safety net) and **interrupt-driven** (VDP int on, the beat calls a handler — free background work, but interrupts land mid-code). Separate UPDATE (CPU-RAM computation, safe anytime) from RENDER (VRAM writes, batched into the vertical-blank SAFE WINDOW after F). Dirty-rectangle: render only what changed. Time system = a frame counter (60/sec) + countdown timers = scheduled events | Ch. 17 §17.2/§17.3/§17.8 | loop code assembles; F-wait needs the running beam (Ch. 22); budget from the measured 50k |
| VDP address-register race | THE classic TI crash: aiming the VDP counter takes TWO port writes (low byte, high byte); if a VDP interrupt fires between them, the ISR touches the chip (reads status, resets the byte flip-flop, moves the counter) and the main loop's second byte then lands wrong -> corruption/crash, unreproducible under single-step (the interrupt never lands in that 2-instruction window when stepping). Fix: mask interrupts around every VDP access (LIMI 0 / LIMI 2), or keep the ISR off the VDP. Every counter-aim is a critical section | Ch. 17 Pitfalls | described from the port protocol (Ch. 12) + firmware ISR behavior; formal masking discipline in Ch. 22 |
| Ch. 17 code artifacts | `code/ch17/`: terrain.a99 (coarse name-table scroll + budget measurement), smooth.a99 (pattern-shift smooth scroll) | Ch. 17 | both assemble via `sh verify.sh` (44 sources); scroll mechanics + costs verified; commit 0d3e5d5 |
| Mid-frame register trick (split screen) | the beam draws each scanline from LIVE registers, so changing a register partway down the frame draws the top and bottom under different modes — e.g. a bitmap play-field with a text panel below. Fragile on the 9918A: NO scanline interrupt, so the seam must be hit by cycle-counting from the frame interrupt or the 9901 timer (Ch. 22); a stray interrupt jitters it | Ch. 18 §18.1 | modeled by the project's beam-accurate render_line (reads live regs per line); demonstrating needs the running beam (desktop), not the whole-frame bench render |
| Status-read hazards | reading VDP status (>8802) CLEARS F/5S/C and RESETS the address flip-flop. So: (1) two readers lose data — with the console ISR reading status every frame, your direct read of the sprite flags races and usually loses; read the ISR's mirrored copy instead; (2) an ISR status-read mid-address-setup corrupts the counter (the §17.4 race). Etiquette: with interrupts on, the status register is the ISR's to read; mirror what you need; mask around atomic VDP access | Ch. 18 §18.2 | vdp.rs read_status (&=0x1F); firmware ISR behavior |
| VDP mode-bit archaeology | R1 bit7 = 4K/16K select (the 4A wires + sets 16K; 4K mode wraps the counter at 4 KiB — the cramped-addressing world of a forgotten bit). R0 bit0 = external-video overlay enable (unused on a stock 4A; the 9918A's genlock life). Undocumented corners (illegal mode combos degrade text-like; edge-sprite/status-low-bits details): know they exist, don't depend, verify vs real HW/Classic99/MAME | Ch. 18 §18.3 | project models 16K; 4K/ext-video are archaeology (R-12) |
| NTSC/PAL + artifact colour | the 9918A emits COMPOSITE, not RGB — high-frequency pixel patterns decode on a real NTSC TV as **artifact colours** outside the 15-colour palette (a system property: chip+signal+TV, absent from the project's clean digital palette). NTSC = 60 Hz / ~50,000-cyc frame (the book's baseline); PAL = 50 Hz / ~60,000-cyc frame (more cycles/frame, but frame-counted timing runs 5/6 as fast — a porting rescale). Project models NTSC | Ch. 18 §18.4 | R-12 (project = digital NTSC); PAL/artifact flagged, not modeled |
| VDP successors (targets) | 9938/9958 (V9938, MSX2): backward-compatible + 80-col, up to 128 KiB VRAM, a HARDWARE scroll register, more colours/sprites. **F18A**: FPGA drop-in, cycle/pixel compatible + VGA, more colours, hardware scroll, SCANLINE INTERRUPTS (stable splits), embedded GPU. 4A-first discipline: DETECT the enhanced chip and branch — enhancements where present, Part III techniques where not. Project models the STOCK 9918A; js99er/real HW cover F18A; full F18A = Ch. 34/44 | Ch. 18 §18.5 | R-12 (shelf tools for successors) |
| VDP bandwidth cookbook | measured throughput (bench): frame **~50,000 cyc**; register write (VWTR+operands) **~190 cyc**; one VRAM byte (VSBW, aim+write) **~340 cyc**; bulk VMBW **~73 cyc/byte**; raw aim-once stream **~62 cyc/byte**; full 768-cell name rewrite **~163,000 cyc** (~212/cell); bitmap bring-up+clear (13,056 B) **~870,000 cyc** (~66/byte); pattern-shift scroll (8 B) **~1,044 cyc**. Derived: **~700–800 VRAM bytes/frame**. The AIM is the tax (~300 cyc); law = **aim once, stream many** | Ch. 18 §18.6 | machine-verified (bench cookbook.a99 + ledgered Part III measurements), commit 0d3e5d5 |
| Ch. 18 code artifacts | `code/ch18/`: cookbook.a99 (measures VWTR/VSBW/VMBW/raw-stream costs for the §18.6 table) | Ch. 18 | assembles via `sh verify.sh` (45 sources); costs verified; commit 0d3e5d5 |
| SN76489 sound chip | 3 square-wave TONE channels (0-2) + 1 NOISE channel (3), commanded through the WRITE-ONLY port >8400 (SNDWR). LATCH byte (bit7=1: `1 cc t dddd` — channel cc, type t (0=period/noise-ctrl, 1=attenuation), low 4 data bits); DATA byte (bit7=0: `0 dddddd` — high 6 bits of the last-latched tone period). Tone period = 10-bit N; attenuation = 4-bit. **Writes only reach the chip at EVEN addresses** (the console's wsndbyte discards addr&1) | Ch. 19 §19.1 | machine-verified (bench, sndlib STONE; psg.rs), commit 74c9b90 |
| SN76489 frequency math | tone frequency **f = 3,579,545 / (32*N)** (master clock = NTSC colourburst, /16 divider, /2 square wave). N=1..1023 -> ~3494 Hz down to ~109 Hz (~5 octaves). A4 440 Hz -> N=254 (verified 440.4 Hz). Note table = one octave of 12 dividers (C4=428..B4=226), equal temperament (2^(1/12)), all within +/-0.25%; octave up = N>>1, down = N<<1 | Ch. 19 §19.2 | machine-verified (bench `sound`: C4 261.4/E4 330.0/G4 392.5 Hz chord), commit 74c9b90; generator = Ch. 38 asset script (no python on PC box) |
| SN76489 attenuation + noise | attenuation 4-bit: **0 = loudest, 15 = silent, 2 dB/step**. Envelopes = a table of attenuations stepped per frame (pluck/swell/decay). Noise (ch3) = 15-bit LFSR: ctrl bit2 = white vs periodic; low 2 bits = shift rate (3 fixed, or 3 = follow tone-2's period, tunable). Drums/engines/explosions = noise + rate + decay envelope | Ch. 19 §19.3/§19.4 | machine-verified (bench `sound`: att levels; white noise att 8), commit 74c9b90 |
| sndlib module (lib99) | `code/ch19/sndlib.inc`: SSILNC (all 4 channels att 15), STONE (R0=ch, R1=N, R2=att -> latch period + data + att latch), SVOL (R0=ch, R1=att), SNOISE (R0=ctrl, R1=att); NOTES table (C4..B4 dividers). Leaves, clobber R3/R4. Effects (pitch slide/vibrato/arpeggio/zap = sfxlib), the console sound-list format (duration-tagged register streams + ISR auto-player, Ch. 22), and a tracker/pattern music driver with channel-stealing are §19.5-19.8 | Ch. 19 §19.1/Lab | machine-verified (bench: C-major chord + white noise read back correct), commit 74c9b90 |
| bench `sound` command | BENCH99 gains `sound` (alias `snd`): dumps the SN76489 PSG state — per tone channel N/frequency(Hz)/attenuation + noise white/periodic + attenuation — read through Psg's diagnostic accessors (the chip is write-only, so >8400 reads are open bus). The sound-work oracle (like `vram`/`pixels` for graphics) | Ch. 19 tooling | added commit 74c9b90 (`code/bench/src/main.rs`); uses only libre99-core public API (bus.psg + Psg accessors) |
| Ch. 19 code artifacts | `code/ch19/`: sndlib.inc + sndlib.a99 (sound engine + chord self-test) | Ch. 19 | both assemble via `sh verify.sh` (46 sources); chord + noise verified via `sound`; commit 74c9b90 |
| Speech synthesizer hardware | the sidecar Speech Synthesizer unit = the **TMS5200** (cousin of the TMS5220) + a speech ROM (resident vocabulary). Memory-mapped: **>9000** read (status/data, SPCHRD), **>9400** write (commands/data, SPCHWR). Command bytes select reset / load-address / read / speak-resident / **Speak External** (stream LPC data through a ~16-byte FIFO). Status bits: talking, buffer-low, buffer-empty — poll to pace streaming without starving the frame (Ch. 17). Descends from the 1978 Speak & Spell (Frantz/Wiggins/Breedlove/Brantingham) | Ch. 20 §20.1/§20.2 | **R-12: NOT emulated by the project** (>9400 writes ignored, >9000 reads open-bus >00, both bench-confirmed); protocol datasheet-derived (App. F); MAME/js99er voice it |
| LPC (linear predictive coding) | speech = a **source-filter model**: a source (voiced buzz at a pitch, or unvoiced hiss) shaped by a vocal-tract filter (10 reflection coefficients K1-K10 on the TMS5200). One **frame** (~25 ms, ~40/sec) = energy + pitch + K1-K10, interpolated between frames -> **~1,200 bits/second** (a fortieth of raw audio). Works because speech is linearly predictable (a low-order resonant filter) — store the model, not the samples. The foundation of all speech coding | Ch. 20 §20.4 | theory (Atal et al.); the compression trick behind modern codecs/neural vocoders |
| Speech: vocabulary / new speech / allophones | resident vocabulary = a few hundred baked-in words/phrases, spoken by index and CHAINED into sentences (even cadence -> the TI speech sound). NEW speech = record -> LPC-encode (BlueWizard / python_wizard, PC-side, python) -> stream via Speak External. ALLOPHONE route (Terminal Emulator II) = text -> phoneme string -> unlimited but robotic. Trade: natural-but-canned vs unlimited-but-robotic. Integration: feed the FIFO ~a couple bytes/frame + DETECT the unit and degrade gracefully | Ch. 20 §20.3/§20.5/§20.6/§20.7 | encoders need python (absent on PC box); described from record/datasheet |
| spklib module (lib99) | `code/ch20/spklib.inc`: SPKWR (write byte -> >9400), SPKRD (read >9000), SPKDET (detect the unit: run the read-signature protocol, >AA present / open-bus absent -> R0 >FFFF/>0000). The bench-verified fact = **SPKDET returns ABSENT (>0000) on the project's no-speech console** (green verdict); speak/stream command bytes are datasheet-derived (App. F), correct but unvoiceable here | Ch. 20 Lab | machine-verified DETECTION (bench: SPKDET absent -> R7=>02 green; m 9000 = 00 stub), commit 75ca53e |
| Ch. 20 code artifacts | `code/ch20/`: spklib.inc + spklib.a99 (speech interface + detection self-test) | Ch. 20 | both assemble via `sh verify.sh` (47 sources); detection verified (absent, green); commit 75ca53e |
| TMS9901 (input role) | one interface chip at CRU base 0, three jobs: interrupt controller (Ch. 22), I/O pins, interval timer (Ch. 22). Input = an 8x8 SWITCH MATRIX (keys + joysticks), no controller chip. Scan: drive a 3-bit column onto the 9901 select pins (LDCR 3, COLSEL R12=>0024), read 8 rows back (STCR 8, ROWRD R12=>0006). Switches ACTIVE LOW (0 = pressed). Ch10's READROW/DECROW/KSCAN | Ch. 21 §21.1/§21.2 | machine-verified (ch10 scan + ch21 inplib), commit bb565e4 |
| Joystick read | joysticks = matrix columns 6 (joy1) / 7 (joy2); rows: 0 fire, 1 left, 2 right, 3 down, 4 up (active low). inplib IREAD = READROW on col 6+n, inverted to active-HIGH mask (bit0 fire, 1 left, 2 right, 3 down, 4 up). Diagonals free (independent bits). Verified with the new `press`: J1U -> >0010, J1U+J1F -> >0011, idle -> >0000 | Ch. 21 §21.4 | machine-verified (bench press + IREAD), commit bb565e4; matches keyboard.rs cols 6-7 |
| Alpha Lock trap | the Alpha Lock (caps latch) shares a matrix line with joystick-1 UP; latched down it holds that line so joystick-up misreads (drift up / can't climb) — the platform's most infamous input bug. Fix: read the Alpha Lock state and compensate, or tell the player to release it. **NOT modeled by the project keyboard (R-12)** — every switch is independent; Classic99/MAME reproduce it | Ch. 21 §21.4 | R-12 gap (hardware record); described, not demonstrated on the bench |
| inplib module (lib99) | `code/ch21/inplib.inc`: IREAD (joystick R0 -> active-high mask), IEDGE (R0=prev, R1=cur -> R2=pressed = cur AND NOT prev, R3=released = prev AND NOT cur, via SZC; HELD = cur AND prev). Per-frame edge detection = the event model (press/held/release) + free debounce (frame-to-frame only). Redefinable keys = an action->switch mapping table (name actions, map once) | Ch. 21 §21.5/Lab | machine-verified (bench: IEDGE cases + IREAD real presses), commit bb565e4 |
| Input conventions | KSCAN (firmware) scans the whole matrix + shift/repeat/debounce, returns ONE key — good for text/menus, not simultaneous game inputs (roll your own). QUIT (FCTN-=) hard-resets the console; serious games DISABLE it (a scratchpad flag) + provide a safe pause. Two-player = joy1+left-keyboard / joy2+right-keyboard (split KSCAN modes); support joystick OR keyboard | Ch. 21 §21.3/§21.6 | KSCAN/QUIT = firmware/E-A manual; conventions = period practice |
| bench `press`/`rel` | BENCH99 gains `press <name>` / `rel <name>`: set a key or joystick switch (J1U/J1D/J1L/J1R/J1F, J2..) on the CURRENT bench (bare or boot) via bus.keyboard.set_key, then return — so code scanning the matrix on the bare bench reads it (unlike `k`, which taps + runs frames in boot mode only). The input oracle | Ch. 21 tooling | added commit bb565e4 (`code/bench/src/main.rs`); uses only libre99-core public API |
| Ch. 21 code artifacts | `code/ch21/`: inplib.inc + inplib.a99 (input layer + self-test) | Ch. 21 | both assemble via `sh verify.sh` (48 sources); IEDGE + IREAD (real presses) verified; commit bb565e4 |
| 9900 interrupts + console wiring | the TMS9900 has 16 priority interrupt LEVELS (each a WP:PC vector in low RAM, a hardware BLWP). The 4A wires everything enabled to **level 1** (no priority encoder) + RESET (level 0, power-on/QUIT) + LOAD (non-maskable, the debug back door). **LIMI n** = set the CPU interrupt mask (LIMI 0 = off, LIMI ≥1 = level-1 on). Sources into level 1 via the 9901: the VDP vertical interrupt (60 Hz) and the 9901 timer | Ch. 22 §22.1/§22.2 | vdp_interrupt on /INT2 + mask (cru.rs); LIMI in cpu.rs |
| Console ISR (per frame) | the firmware installs a level-1 handler that runs EVERY frame (60/sec): KSCAN (keyboard), automatic sprite motion, sound-list auto-player service, QUIT check, screen-blank timeout, and a frame counter. It reads VDP status each frame (clears F — the ack). Costs a slice of the ~50k-cycle frame and touches the VDP behind your code (the §22.8 race). Frame counter at pad **>8379** | Ch. 22 §22.3 | machine-verified (bench boot: >8379 = >3C exactly after `f 60`), commit bb565e4 |
| User ISR hook >83C4 | the firmware ISR checks pad **>83C4** each frame for a pointer to a USER interrupt routine and calls it if non-zero — how your code runs 60/sec without owning the interrupt. Contract: be fast (spends the frame budget), preserve firmware/GPL/interrupted state, return cleanly; CHAIN by calling the previous hook. Ideal for a music driver / timer / background task | Ch. 22 §22.4 | **machine-verified** (bench boot: a poked INC/RT routine at >83C4 fires per frame — 59 counts over `f 60`), commit bb565e4 |
| 9901 interval timer | a 14-bit down-counter (system clock / divider) that raises level 1 (or a pollable flag) on underflow, then reloads — programmed via 9901 timer mode (CRU bit 0). Uses: music tempo independent of 60 Hz, mid-frame splits (Ch. 18), off-rate sampling. **NOT emulated by the project (R-12, ROADMAP §6)** — a program's timer writes are inert; on-machine profiling awaits it. A roadmap item (enables timer code + the cassette transport that depends on it) | Ch. 22 §22.6 | R-12 gap (cru.rs: timer mode noted, counter unimplemented); described |
| Profiling (bench cycles) + lib99 costs | on-machine profiling wants the 9901 timer (absent), so profile with BENCH99 `cycles` bracketing labeled checkpoints (PROFILE99). Measured lib99 call costs (incl. operand setup): IEDGE ~156, VWTR ~190, SVOL ~256, SSILNC ~260, IREAD ~294, VSBW ~340, STONE ~560; VMBW ~73/byte. With the ~50k frame, budgeting = arithmetic | Ch. 22 §22.7 | machine-verified (bench, profile99.a99 deltas; graphics rows = Ch. 18 cookbook), commit bb565e4 |
| Critical sections (the cure) | any state shared with the resident ISR — the VDP address counter above all — is a CRITICAL SECTION. The VDP address race (Ch. 17): an interrupt landing between the two address-aim port writes lets the ISR move the counter, so the 2nd byte lands wrong. **Cure: mask around it — LIMI 0 before the aim, LIMI 2 after.** Every VWA-based lib99 routine is a critical section; poll-only (LIMI 0 always) sidesteps the race | Ch. 22 §22.8 | described (formalizes Ch. 17 Pitfalls); the masking idiom |
| Ch. 22 code artifacts | `code/ch22/`: profile99.a99 (the cycle-profiling harness for lib99 calls) | Ch. 22 | assembles via `sh verify.sh` (49 sources); STONE/SVOL/SSILNC/IREAD/IEDGE costs measured; the >83C4 hook + frame counter verified in boot mode; commit bb565e4 |
| Console ROM services + doorways | the console ROM/GROM holds a library (char-set loaders, floating point, cassette, KSCAN, utilities). Two doorways from assembly: **GPLLNK** (BLWP linkage that runs a GPL-side routine from GROM via the interpreter — char sets, cassette) and **XMLLNK** (runs a ROM machine-code routine from a table by code, or direct-address XML — faster, the FP door). Pattern: linkage + a DATA word naming the routine + scratchpad setup | Ch. 23 §23.1-§23.3 | project GROM implements GPL services (e.g. >004A char-set loader, libre99-gpl/src/font.rs); calling from assembly is E/A/GPL-env dependent (§23.6) |
| Radix-100 floating point | TI reals = **radix-100**: 1 exponent byte (power of 100, biased) + 7 mantissa bytes, each a base-100 digit (0-99, sign folded in) = 8 bytes. Decimal-friendly (typed/printed numbers match stored, no binary round drama). Operates on **FAC** (>834A) + **ARG** (>835C): add/sub/mul/div/compare (XMLLNK each, result in FAC) + CIF/CFI (int<->float) + number<->string. TI BASIC = this package + a parser | Ch. 23 §23.4 | E/A manual; format spotted in VDP RAM (Field Notes); FP ops not exercised on the bench (E/A-env) |
| FP vs fixed-point judgment | the FP package is general (any magnitude, 7 digits) but costs hundreds-to-1000+ cyc/op (E/A manual) — a luxury in a 50k frame. Games use **fixed point** (Ch. 8/16): FXMUL (8.8 x 8.8 via one MPY + shift) verified 1.5*2=>0300, 2.5*2.5=>0640, 0.5*0.5=>0040 at **~220 cyc/call** — several times cheaper, accurate enough for game ranges. Rule: FP only for genuine range/decimal-precision needs | Ch. 23 §23.5 | machine-verified (bench, fxcalc FXMUL: results + ~220 cyc), commit bb565e4; FP cost = manual |
| Environment honesty (E/A doorways) | GPLLNK/XMLLNK are **Editor/Assembler utilities** — E/A installs the linkage vectors in low RAM when it loads your program. A BARE CARTRIDGE (booting from the menu, no E/A image) has NO such vectors: `BLWP @GPLLNK` jumps into uninitialized memory and crashes. Fix: (1) re-implement minimal GPLLNK/XMLLNK SHIMS installed by your cart (Ch. 35), or (2) avoid the services — ship your own font + fixed-point math (what lib99 does) | Ch. 23 §23.6 | the key ch23 caveat; lib99 is self-contained by design |
| Ch. 23 code artifacts | `code/ch23/`: fxcalc.a99 (8.8 fixed-point multiply FXMUL — the verified alternative to the ROM FP package) | Ch. 23 | assembles via `sh verify.sh` (50 sources); FXMUL results + cost verified; commit bb565e4 |
| Scratchpad atlas | >8300-83FF = 256 bytes, the ONLY 16-bit 0-wait fast RAM (Ch. 5 fast island) — fastest + most POLITICAL memory (GPL, KSCAN, ISR, FP, E/A, your code all want it). Map: **free lower pad** >8300-836F (workspace, stack, hot vars); **firmware variable zone** ~>8370-83BF (frame counter >8379, user hook >83C4, FAC >834A/ARG >835C, GPL status >837C, KSCAN key+status); **system workspaces** >83C0 (interrupt/utility) + >83E0 (GPL VM — sacred while GPL runs). Choosing where the workspace sits = register allocation by hand | Ch. 24 §24.1/§24.2 | landmarks verified across the book (>8379/>83C4 Ch.22, FAC/ARG Ch.23); ISR footprint probed (bench boot) |
| Pad occupancy by environment | what you may steal DEPENDS on the environment: **bare console** (poll-only, no ISR/GPL) — nearly all 256 yours; **E/A-hosted** — E/A's utility area reserved; **GPL alive** — >837C + >83E0-83FF (GPL workspace) + GPL vars + the ISR's counters/>83C0 all in use, only the low pad free. Rule: know your environment, PRESERVE what it owns. lib99 names its few pad words + touches no others -> composes into any environment | Ch. 24 §24.3/§24.4 | the pad-safety discipline; lib99 layout: WS >8300, stack from >8340, CURPOS >8342 / CUR40 >8346 below the firmware zone |
| Running code in the pad | the pad executes code fast too (16-bit 0-wait fetch), so hot inner loops are staged into it: a startup LOADER copies the kernel from ROM/disk into a reserved 40-80 byte pad patch and runs it there — fastest execution, at the cost of those fast bytes (Ch. 37 perfects it) | Ch. 24 §24.5 | technique (previews Ch. 37) |
| PADWATCH | Lab 24: snapshot the 256-byte pad, run a target, DIFF -> count of changed bytes + first changed address. Key trick: PADWATCH's workspace is OUTSIDE the pad (>A000) so the diff is the TARGET's footprint only. The empirical "what did that touch?" oracle for the fast island | Ch. 24 Lab | machine-verified (bench: a >8340 write -> NCHG=2, FIRST=8340), commit bb565e4 |
| Ch. 24 code artifacts | `code/ch24/`: padwatch.a99 (scratchpad snapshot+diff tool) | Ch. 24 | assembles via `sh verify.sh` (51 sources); footprint diff verified; commit bb565e4 |
| GROM (the strange memory) | TI's TMC0430: a 6 KiB READ-ONLY chip with NO address bus — an internal 16-bit address counter, streamed through 4 ports. **>9800** GRMRD read data (auto-increment), **>9802** GRMRA read counter (hi then lo), **>9C00** GRMWD write data (GRAM only; mask ROMs ignore), **>9C02** GRMWA write counter (hi then lo). Read = aim (address hi byte then lo to >9C02) then stream from >9800. Serial-memory economics (density/cost/TI proprietary control) | Ch. 25 §25.1/§25.2 | machine-verified (bench, gromlib reads console GROM header AA 02 00 00 00 00 00 00 13 10 13 20 = od roms/994AGROM.Bin), commit c970196; grom.rs |
| GROM prefetch (the off-by-one) | the chip keeps a 1-byte prefetch buffer, filled AUTOMATICALLY the instant the low address byte is written: after aiming at A, buffer=mem[A] and the counter=A+1. First >9800 read returns mem[A] (correct) + refills; but reading the address port back shows **A+1**, not A (the counter leads the data by one). Not a bug — the pipeline. Code reading the address to find "where am I" must remember the +1 | Ch. 25 §25.2 | machine-verified (bench, gromlib streams correctly incl. the prefetch), commit c970196; grom.rs read_data/write_address |
| GROM space + bases + GRAM | 64 KiB counter space = eight 8 KiB SLOTS (6 KiB devices, top 2 KiB unused); auto-increment WRAPS within a slot (no silent cross-device reads). Console GROMs 0-2 = OS/menu/TI-BASIC/GPL library; cartridge GROMs 3-7 scanned at boot (Ch. 28). **16 parallel GROM BASES** (>9800, >9804, ... 4 bytes apart) for peripherals (p-code card). **GRAM** = writable GROM-space devices (GRAM Kracker, FinalGROM 99, the emulator) — the door to run our own GPL (Ch. 27) | Ch. 25 §25.3/§25.4/§25.5 | grom.rs (8K slot wrap, 64K space); GRAM = writable path for Ch. 26-27 |
| gromlib module (lib99) | `code/ch25/gromlib.inc`: GRDADR (aim the counter at R0 — hi byte then lo), GRDBYT (read one byte -> R1 hi, auto-inc), GRDBLK (stream R2 bytes to CPU dest R1). SAVE/RESTORE discipline (§25.6): the GROM counter is SHARED state — for the GPL interpreter it IS the instruction pointer — so a polite reader saves it (read >9802 x2, which also resets the write phase) + restores it, or it derails live GPL | Ch. 25 §25.6/Lab | machine-verified (bench: header + GROMDUMP), commit c970196 |
| GROMDUMP (Lab 25) | streams 256 console-GROM bytes through gromlib + 16-bit checksums them (sum mod 2^16) = **>50E7** — the same value `od roms/994AGROM.Bin` sums over its first 256 bytes, proving gromlib faithful at scale (not just the header). A GROM fingerprint tool (tell console versions apart, verify a dump) | Ch. 25 Lab | machine-verified (bench: CKSUM=>50E7, R7=>02), commit c970196 |
| Ch. 25 code artifacts | `code/ch25/`: gromlib.inc + gromlib.a99 (GROM reader + header self-test), gromdump.a99 (checksum) | Ch. 25 | assemble via `sh verify.sh` (53 sources); header + checksum verified vs the ROM file; commit c970196 |
| GPL = a bytecode VM in ROM | the console has a SECOND, virtual processor: **GPL** (Graphics Programming Language), a bytecode interpreted by a 9900 program in the console ROM. TI wrote the OS, menu, TI BASIC, and most carts in it. Its "machine" = the whole console: CODE in GROM (the interpreter's PC is a GROM address), SCREEN+data in VDP RAM, VARIABLES in the scratchpad. Chosen for GROM density, planned-family portability, dev speed — at the cost of interpreter slowness | Ch. 26 §26.1 | machine-verified (bench: the emulator's genuine interpreter runs GPL; gromlog traces it), commit 9aeb723 |
| GPL execution model + cost | fetch-decode-execute loop over GROM bytes. GPL registers/PC live in the **>83E0** workspace; status byte at **>837C** (branches test it). Each GPL instruction = MANY 9900 instructions (fetch, jump-table decode, execute) + one+ slow GROM reads -> roughly an ORDER OF MAGNITUDE over the equivalent 9900 op. The project targets FRAME parity, not cycle-exact GPL timing (rom_perf.rs) | Ch. 26 §26.2/§26.8 | machine-verified shape (bench: `gromlog` shows the interpreter fetching GPL >018F-01A6; libre99gpl dis decodes ST/XML/DECT/RTN); precise per-op cycles = project non-goal |
| GPL addressing (space tags) | operands are SPACE-TAGGED: `>NN` immediate, `V@addr` VDP RAM, `@addr` CPU RAM (pad), `G@label` GROM, `*@addr` indirect (pointer in addr); + indexed/register-relative (App. B). One instruction can span several memories — the interpreter reaches each | Ch. 26 §26.3 | machine-verified (libre99gpl dis: `ST V@>0000,>48`, `MOVE >0005,G@MSG,V@>0002`, `ST *@>8372,>01A2`), commit 9aeb723 |
| GPL instruction set | data: **MOVE** (the block-mover between ANY two memories — GPL's superpower; GROM->VDP in one op), ST, ADD/SUB/MUL/DIV/INC/DECT, logic, compares. Flow: B / BR / BS (branch on status set/reset = the `if`), CASE/DCASE (switch), CALL/RTN/FETCH (subroutine + GPL data stack), EXIT. Console verbs: **FMT** (a screen-layout DSL inside an opcode), ALL (screen fill), BACK (border), SCAN (keyboard), RAND, sound-list dispatch | Ch. 26 §26.4-§26.6 | machine-verified (libre99gpl dis of console.gpl + demo.gpl round-trip: ALL/ST/MOVE/RTN), commit 9aeb723 |
| GPL sweet spots (the judgment) | GPL is SUPERB for human-speed software (menus, UI, text, attract, data-driven flow — the ~10x interpreter tax is invisible) and HOPELESS for machine-speed inner loops (per-frame motion/collision/sound — the tax blows the 50k frame). Rule: **GPL for human speed, 9900 for machine speed** -> TI's action games are HYBRIDS (GPL shell + 9900 core via XML, Ch. 29; the console GPL itself calls `XML >1A` at >0198) | Ch. 26 §26.8 | the design behind Ch. 27-29 |
| Ch. 26 code artifacts + tooling | `code/ch26/`: demo.gpl (a tiny GPL program, round-trips through libre99gpl asm->dis byte-exact). BENCH99 gained `gromlog` (trace the interpreter's GROM-fetch stream — the GPL "instruction bytes" the 9900 trace's counterpart) | Ch. 26 Lab | machine-verified (demo.gpl round-trip; gromlog on booted menu -> GPL fetches >018F-01A6); libre99gpl asm/dis; commit 9aeb723 |
| libre99gpl toolchain | the project's GPL assembler/disassembler (built the clean-room console GROM). `libre99gpl asm <src.gpl> <out.bin>` (two-pass), `dis <grom.bin> [addr]`, `console <out.bin>`. Source = space-tagged operands + directives GROM/AORG (origin; GPL addressing is ABSOLUTE, unlike libre99asm's forced >6000), BYTE/DATA/TEXT, labels. Round-trips (demo.gpl/quizmaster.gpl asm->dis byte-exact). xdt99's xga99 = the community's GPL assembler | Ch. 27 §27.1 | machine-verified (round-trips), commit d81f2db |
| GPL cartridge/program header | a cartridge GROM begins with the **>AA signature** + version + 6 list pointers (power-up, program, DSR, subprogram, interrupt, reserved). The PROGRAM LIST points to a linked list of entries, each = {next-ptr (0=last), entry-address, name-length byte, name TEXT}. The master menu scans GROM bases for >AA, walks the list, lists "n FOR NAME", and jumps to the entry address on the keypress. Verified: quizmaster.gpl -> AA 02 01 00 + list ptr >2010 + entry {next 0000, >201B, 06, "QUIZ99"}; booted menu lists "1 FOR TI BASIC" by the same scan | Ch. 27 §27.2 | machine-verified (bench: quiz.bin header bytes + booted-menu discovery), commit d81f2db |
| GPL program structure | four idioms: draw screens with MOVE (or FMT) from GROM TEXT/BYTE to VDP; plan VARIABLES in the scratchpad (name pad words, clear of >83E0/>83C0); factor with CALL/RTN (interpreter data stack); drive input with a SCAN loop. Multi-GROM (>6 KiB): code GROM + data GROM, cross-slot refs explicit (auto-inc wraps in a slot), base-address hygiene. quizmaster entry = ALL/MOVE/SCAN/BR | Ch. 27 §27.3/§27.4 | machine-verified (bench: quizmaster entry disassembles ALL/MOVE/SCAN/BR), commit d81f2db |
| GPL cartridge tooling gap (roadmap) | **R-12**: libre99gpl builds CONSOLE GROMs (24 KiB, 0-5FFF, replaces 994AGROM.Bin) but NOT plug-in cartridge GROMs at >6000, nor .ctg/.rpk packaging (asm at >6000 = "past the 24 KiB image"). So ch27 builds in console-GROM space + demos discovery via the console's own menu. Building/shipping a cartridge GROM = a project ROADMAP item (libre99gpl needs a cartridge-image mode + .ctg emitter). Shelf tools: xdt99 xga99, Classic99. Shipping formats: .ctg (ti99sim, GROM pages 3+), FinalGROM 99, Classic99, MAME .rpk | Ch. 27 §27.5 | R-12 gap surfaced (the book driving the emulator, as the outline predicts) |
| GPL debugging | two machines at once: `libre99gpl dis` reads the GPL; BENCH99 `gromlog` traces the interpreter's GROM-fetch (GPL) stream; `s` traces its 9900 steps. Classic99 = GPL-aware single-stepper + GROM viewer (shelf). Classic bugs: status-byte confusion (a branch tests a status changed by an intervening op) and MOVE direction slips (source/dest spaces transposed -> copies the wrong space) | Ch. 27 §27.6 | described (uses the verified gromlog + dis of Ch. 26) |
| Ch. 27 code artifacts | `code/ch27/`: quizmaster.gpl (a GPL program with the cartridge/program header + a MOVE/SCAN entry) | Ch. 27 Lab | machine-verified (bench: header bytes + entry disassembly + menu-discovery mechanism); the .a99 sweep stays green (53 sources); commit d81f2db |
| Power-on / boot into GPL | RESET (level 0) loads WP:PC from the vector at >0000-0003 (console ROM) -> 9900 hardware init (VDP regs, clear VRAM, silence sound, prep pad) -> the LEAP INTO GPL: init starts the interpreter + points it at the boot GPL. On the clean-room GROM the ROM jumps to GROM **>0020** (BR >0052 -> the real boot code), with the interconnect BR-stub table below (>0022+). From that instant the OS IS a GPL program (draws the title with MOVEs) | Ch. 28 §28.1 | machine-verified (bench: libre99gpl dis >0020 = BR >0052; boot reaches the clean-room title M1), commit 163fc84 |
| Master menu algorithm | for each GROM base/cartridge slot: peek byte 0 for the **>AA signature**; where found, follow the header's program-list pointer + walk the linked list of {next, entry-addr, namelen, name}; build the "n FOR NAME" list; DISPATCH the chosen entry on the keypress (GPL prog via the interpreter sub-stack, ML prog via XML). Verified: booted clean-room console lists "1 FOR TI BASIC" — a program DISCOVERED by the scan (the mirror of the Ch. 27 header). No privileged built-ins — only entries it found | Ch. 28 §28.2 | machine-verified (bench: M2 menu "1 FOR TI BASIC"), commit 163fc84 |
| Power-up hooks + GPL-side services | the header POWER-UP LIST runs code BEFORE the menu (peripheral init; and the classic abuse — a cart auto-starting to bypass the menu). Much of the "OS personality" is GPL-side: character sets (GPL data MOVEd by GPL code, Ch. 23 GPLLNK), cassette dialogs, KSCAN's repeat/modifier personality. The console is a GPL APPLICATION SUITE; the 9900 ROM is only the thin hardware-init + interpreter layer | Ch. 28 §28.3/§28.4 | console.gpl (PUCALL/SFAR power-up walk) |
| TI BASIC = a GPL artifact (2-interpreter tax) | TI BASIC is a GPL PROGRAM that interprets BASIC tokens. A line is CRUNCHed (keywords->1 byte, numbers->radix-100 floats) and stored — with the variables — in **VDP RAM** (behind the video port; that's where the space was). EXECUTE: the GPL BASIC interpreter fetches a token (from VRAM, byte-by-byte through the port), decodes, runs GPL, which the 9900 interprets. **TWO interpreters stacked** + a VDP-port access per variable -> ~TWO ORDERS OF MAGNITUDE slower than 9900 assembly. THE complete explanation of TI BASIC's infamous slowness | Ch. 28 §28.5 | described from the record (Ch. 26 GPL tax measured; BASIC-over-GPL + VDP-port compounding); the book does NOT teach TI BASIC as a language |
| Clean-room console GROM | the project boots a CLEAN-ROOM console GROM (console.gpl) — an original OS in GPL, no TI copyrighted bytes, on the GENUINE GPL interpreter, via --system-grom: original title, cartridge scan/dispatch, documented pad map. Proves the boot contract of §28.1-28.3 is FULLY SPECIFIED + answers the PRESERVATION question (a freely-licensed GROM keeps the platform runnable without copying TI's bytes) | Ch. 28 §28.7 | machine-verified (the whole boot: title M1 + menu M2 run the clean-room GROM); the book-founds-on-the-project thesis |
| Console versions | "the 4A" isn't one machine: 99/4 (1979, different GROM OS + calc keyboard) vs 99/4A (1981, this book's target) vs 4A GROM revisions (v2.2 etc., small behavioral diffs). Use DOCUMENTED entry points/headers not raw addresses; detect capabilities. The clean-room GROM is one more (documented) "version" — booting the same software proves the CONTRACT matters, not the bytes | Ch. 28 §28.6 | Ch. 44 (family) |
| Ch. 28 (no new code artifact) | boot-trace chapter: uses libre99gpl dis (console GROM >0020), BENCH99 boot + gromlog + m 8300 (birth certificate), and console.gpl | Ch. 28 | machine-verified (boot path + menu), commit 163fc84 |
| TI hybrid architecture (the house pattern) | the machine's best software = HYBRIDS: a **GPL shell** (menu/title/text/human-speed flow) wrapped around a **9900 core** (motion/collision/sound/inner loop). Sorted by SPEED requirement (human-speed -> GPL; machine-speed -> 9900). Economics: GPL is compact + lives in cheap dense GROM; 9900 is fast + lives in dearer ROM at >6000 — spend cheap GROM on the roomy shell, expensive ROM on the hot core (the "Python for bulk, C for hot loop" calculus in two 1981 memories) | Ch. 29 §29.1 | the pattern behind Part IX; grounded in verified XML/GPLLNK behavior |
| XML (GPL -> 9900 bridge) | **XML** (eXecute Machine Language), a GPL opcode (0x0F) + a 1-byte code selecting a 9900 routine: the interpreter transfers to it (runs NATIVE, full speed, no tax), it returns, GPL resumes. The FFI call. Codes come from the ROM ROUTINE TABLE (console services) or a program's own XML VECTOR TABLE (installed in the scratchpad — wires a cart's GPL shell to its 9900 core). Params cross via the shared scratchpad + agreed registers | Ch. 29 §29.2 | machine-verified behavior (console.gpl: XML >19/>1A power-up scan/call, XML >F0 = the ML-launch trampoline the menu uses; ch26 saw XML >1A at >0198), commit 468ab7e |
| XML >F0 (the ML-launch trampoline) | the master menu launches a MACHINE-CODE (9900) cartridge program with **XML >F0** — GPL dispatching to assembly, the hybrid's defining act. Execution-verified in the project (console.gpl RECON §2/§3) | Ch. 29 §29.2 | machine-verified (console.gpl uses XML >F0, execution-verified), commit 468ab7e |
| Hybrid state contract (both ways) | XML (GPL->9900): the native routine must PRESERVE/restore what the interpreter needs — the GPL workspace >83E0, the GPL PC + data-stack pointer, the status byte >837C — or the interpreter resumes corrupted. GPLLNK (9900->GPL, Ch. 23 mirror): set up the pad params + workspace the GPL side expects; respect the shared scratchpad (>83E0 + the ISR cells >83C0 off-limits to a core coexisting with a live interpreter). Law: what you borrow across the seam, you return unchanged | Ch. 29 §29.3 | the Part V shared-resource principle applied to the language boundary |
| Hybrid packaging + skeleton | a hybrid CARTRIDGE carries BOTH memories: GROM (GPL shell, >AA program header, Ch. 27) + ROM at >6000 (9900 core, >AA ROM header, Ch. 35). Init choreography = who initializes whom: the menu-dispatched side brings the other online (GPL shell installs the core's XML vectors, OR the menu launches the ROM core via XML >F0 + it calls GPL). The **reusable hybrid SKELETON** (quizmaster.gpl shell + a lib99 9900 core, wired via XML, contract honored) = the CHASSIS for Part IX | Ch. 29 §29.4/§29.6 | build = libre99asm core + libre99gpl shell; cartridge-emission = the §27.5 roadmap item |
| Ch. 29 (no new code artifact) | closes Part VI. Composes ch27's quizmaster.gpl (GPL shell) with the measured 9900 kernels of Parts III-V (spritelib/sndlib/inplib). Lab: rebuild QUIZMASTER's timing-critical round as a 9900 core via XML + measure (GPL tax vs cycles) — ~an order of magnitude faster in the core, shell unchanged. Sidebar: Extended BASIC is itself a hybrid (CALL LINK -> user 9900 subprograms; the one fenced CALL LINK page is Ch. 36) | Ch. 29 | grounded in verified XML behavior (commit 468ab7e) + the measured GPL/9900 costs of ch26 + Parts III-V |
| DSR (Device Service Routine) idea | a TI peripheral BRINGS ITS OWN DRIVER — a DSR in ROM on the card — and introduces itself to the console (plug-and-play, 1983). Every card's DSR ROM appears at the shared **>4000-5FFF** window, PAGED IN one card at a time by that card's CRU bit (disk card = CRU base >1100 bit 0; until set, >4000 = open bus). Using a device = 4 beats: FIND (scan CRU bases, check >AA), PAGE IN (SBO 0), CALL, PAGE OUT (SBZ 0). DSR code is position-fixed at >4000, never assumes it's alone | Ch. 30 §30.1 | machine-verified (disk.rs 14 tests; device_io.rs 3 tests — the boot >AA scan finds the disk card @ >1100), GREEN at HEAD; NOT bench (>4000 open bus in BENCH99, R-12 gap) |
| DSR header (>AA + 4 lists) | a paged-in DSR ROM begins with **>AA** at >4000 + version + program count, then 4 linked lists: **power-up** (>4004, run at boot — disk card reserves its VRAM buffer, lowers >8370), **device names** (>4008 — disk card: DSK, DSK1, DSK2, DSK3; node = [link][entry][namelen][name]), **subprograms** (>400A — numbered >10..>16 + named FILES), card ISRs. Name match = string compare on the device name (the part before the first '.': DSK1.MYFILE -> device DSK1). The device sibling of the Ch. 27 cartridge header | Ch. 30 §30.2 | probe-pinned (RECON.md §1/§1b); machine-verified scan (device_io.rs power-up finds the header + reserves VRAM) |
| DSRLNK calling convention | scan CRU bases (>1000, >1100, ... step >0100); SBO 0 pages the DSR in; check >AA; walk the device list at >4008; on a name match call the entry with **R12 = the card's CRU base** + **LIMI 0** (interrupts masked). The DSR **SKIP-returns (R11+2) if it HANDLED** the call, plain-returns to keep searching (the pinned detail). Full disk-DSR drive also needs the PAB staged in the GPL workspace + pad: >8355 name length, >834A name, >8356 VDP addr past the name, **PAB base = [>8356] - devlen - 10** | Ch. 30 §30.4 | probe-pinned (RECON.md §1); machine-verified (device_io.rs: DSK1 link + ToD QUEST load); dsrlib.inc assembles (libre99asm) |
| DSR emulator scope (R-12) | the project models the DISK card at >4000 completely (disk.rs: DSR ROM/CRU >1100/FD1771) + the console 9901; it does NOT model a general multi-card DSR bus — **>4000 is wired to the disk card only**. So the PEB CRU base map (>1100 disk, >1300/>1500 RS-232, ...) is the HARDWARE record; other bases idle. A custom card (NULCARD, answers "NUL.") runs on the FULL-BOX emulators (Classic99, MAME) or real hardware, not this emulator today (a clean roadmap item: a general DSR-card slot + a BENCH99 `dsr` command). Verify disk claims via cargo test / desktop app `--disk`, not BENCH99 | Ch. 30 §30.3/§30.6 | R-12 (emulator models the disk card, not the general bus); shelf = Classic99/MAME |
| Ch. 30 code artifacts | `code/ch30/`: dsrlib.inc (our DSRLNK — CRU-base scan, >AA check, name walk, call w/ skip-return, page-out) + dsrlib.a99 (assembles, entry >607E). NULCARD = the reader's own DSR (real-hardware/MAME) | Ch. 30 Lab | dsrlib assembles via `sh verify.sh` (54 sources); DSR path behavior verified via device_io.rs (green HEAD); NOT bench-runnable (R-12) |
| PAB (Peripheral Access Block) | ALL TI file I/O = one block of bytes IN VRAM you fill in + hand to a device via DSRLNK. Fields: **+0** opcode, **+1** flags (file type/mode in; ERROR OR'd into bits 5-7 out, STICKY), **+2** VDP data-buffer addr (word), **+4** logical record length, **+5** char count, **+6** record number (word, relative files), **+8** screen offset (unused for disk), **+9** name length, **+10** name ("DSK1.FILE"). filelib names the fields (POPCOD/PFLAG/PBUF/...) | Ch. 31 §31.1 | probe-pinned (RECON.md §3); machine-verified (device_io.rs: a real PAB loads ToD's QUEST off a real disk, GREEN HEAD); NOT bench (R-12) |
| PAB opcodes + error codes | opcodes: OPEN 0, CLOSE 1, READ 2 (-> PAB+5=count), WRITE 3 (PAB+5 bytes), RESTORE 4, LOAD 5 (PROGRAM image -> VRAM@PAB+2, no OPEN), SAVE 6, DELETE 7, SCRATCH 8 (disk DSR rejects -> err6), STATUS 9 (-> PAB+8: >08 exists/>80 missing). Errors in PAB+1 bits5-7, STICKY (clear before, check after): **err2** = missing file / reclen mismatch / FIX-on-VAR; **err5** = past EOF (the READ loop's natural exit); **err6** = rejected. Full matrix = App. H | Ch. 31 §31.2 | probe-pinned (RECON.md §3); machine-verified (device_io.rs: real load + graceful bad-device error) |
| TI file types | **DIS/VAR 80** (DV80) = text lingua franca (ASCII, variable records <=80; on disk = [len byte][text] packed in sectors, >FF ends). **DIS/FIX 80** (DF80) = object code (Ch. 6). **INTERNAL** (INT/VAR, INT/FIX) = binary/radix-100 machine data (the catalog is INT/FIX 38). **PROGRAM** = raw memory image, no records (LOAD/SAVE whole; ToD scenario, BASIC progs, Option-5 games). Interchange via xdm99/TIPI by type | Ch. 31 §31.3 | DV80 machine-verified live: FORMATDOC on TI-Write.Dsk (flags >80=VAR, reclen >50=80, 25 recs/25 sectors, cluster @ sector 117, rec 0 = [len 27]"     USING THE TEXT EDITOR") |
| Catalog = a file; VRAM etiquette | CATALOG READ: open the device with a trailing dot + nothing ("DSK1.") as **INT/FIX 38** for input + READ loop; **record 0 = the volume record** (name/size), then one record per file. No special list-dir call. VRAM ETIQUETTE: the PAB + buffers live in VRAM (behind the VDP — where the free memory was), so they must sit CLEAR of the screen tables (Part III) or a read corrupts the display; DSRLNK stages the name pointer at >8356 (PAB base = [>8356]-devlen-10) | Ch. 31 §31.4/§31.6 | probe-pinned (RECON.md: DSK1. catalog INT/FIX 38, rec 0 = volume; >8356 name-ptr) |
| Ch. 31 code artifacts | `code/ch31/`: filelib.inc (PAB field EQUs + opcode/flag EQUs + FDOP + FOPEN/FREAD/FWRITE/FCLOSE wrappers) + filelib.a99 (assembles, entry >6120; COPYs equates/vdplib/dsrlib). Lab: FILER99 (two-pane file manager over textlib40 — described). filelib follows the verified PAB contract; behavior verified via device_io.rs, NOT bench (R-12) | Ch. 31 Lab | assembles via `sh verify.sh` (55 sources); PAB path verified via device_io.rs (GREEN HEAD) |
| Single-density disk geometry | a TI SSSD disk = **40 tracks x 9 sectors x 256 bytes = 360 sectors = 92,160 bytes (90 KB)**, framed by the **FD1771** controller (Ch. 30's >5FF0 registers). Above the physical framing = a flat array of 360 numbered sectors; the filesystem is built in software on it. DSSD = 18 spt/180 KB; Myarc = 80-track/higher | Ch. 32 §32.1 | machine-verified (disk.rs 14 tests GREEN HEAD: FD1771 conformance + geometry) |
| TI filesystem = 4 organs (decoded live) | **VIB** (sector 0 = superblock: name(10), total sectors BE >0A, spt >0C, "DSK" >0D, tracks >11, sides >12, density >13, **allocation bitmap** from >38 = free-space map, LSB-first, 1=used). **FDIR** (sector 1 = inode index: BE sector pointers to FDRs, name-sorted, zero-terminated). **FDRs** (one sector each = inodes: name(10), flags >0C bit0=PROGRAM/bit7=VARIABLE, sectors-alloc BE >0E, reclen >11, **cluster chain** 3-byte descriptors from >1C = extents). Data sectors | Ch. 32 §32.2 | machine-verified LIVE (od Tunnels.Dsk): VIB="TUNNELS"/360/9spt/40trk/1side; FDIR->sectors 2,3; FDR PENNIES=PROGRAM/51sec@34; FDR QUEST=PROGRAM/51sec@85 (= the scenario device_io.rs loads); bitmap 0f 00 00 00 fc... = sectors 0-3+34-135 used |
| SECTOR subprogram (>10) + seclib | beneath files, the disk DSR exposes SUBPROGRAMS by number (not device name); **>10 SECTOR** reads/writes one raw 256-byte sector. Called via the pinned subprogram FRAME (RECON §1): >836D=>0A, >8355=1, >834A=number (>10), params in >834C.. (exact SECTOR param offsets = App. H). seclib SECRD/SECWR wrap it. The primitive for every disk tool | Ch. 32 §32.3 | frame probe-pinned (RECON §1); sector subprogram verified (disk_dsr.rs probe); seclib.inc assembles (entry >60B2); NOT bench (R-12) |
| Controller zoo + .dsk format | TI FD1771 (SSSD 90K baseline) vs CorComp/Myarc (DSSD 180K, 80-track); modern CF7+/nanoPEB (TI FS on CompactFlash), TIPI (Ch. 34). **Capability detection**: read geometry from the VIB, never assume 40x9 (disk.rs PARSES the VIB geometry + honors it; repo's FWeb501.Dsk = DSSD 180K). **.dsk image = sector dump** (all sectors back-to-back, 256B each, no headers, LBA order -> offset = sector*256, so a hex dump IS the disk). Interchange: xdm99/TIImageTool | Ch. 32 §32.5/§32.6 | machine-verified (disk.rs reads .dsk + parses VIB geometry; the live od decode proves offset=sector*256) |
| Ch. 32 code artifacts | `code/ch32/`: seclib.inc (SECTOR subprogram frame + SECRD/SECWR) + seclib.a99 (assembles, entry >60B2). Lab: DISKDOC (sector editor w/ VIB/FDR overlays + corrupt/repair — described). Copy protection was barely present on TI (clean imaging); preservation ethics = image to prevent loss + prefer re-implementation to redistribution | Ch. 32 Lab/§32.7 | assembles via `sh verify.sh` (56 sources); disk structures verified via disk.rs + live od decode |
| RS-232 card + TMS9902 (UNEMULATED) | the RS232 card (a PEB card) = 2 serial ports (each a **TMS9902** UART, a CRU device at base >1300/>1500) + 1 parallel PIO port. Two use levels: **DSR level** (open "RS232.BA=9600.DA=8" / "PIO" + READ/WRITE like a file, Ch. 31) or **register level** (direct 9902 CRU control: LDCR transmit, STCR receive, status bits transmit-empty/receive-loaded; needed for interrupt-driven buffering). **R-12: NOT emulated** (no card at >1300; roms/RS232.Bin unwired) — described from the TMS9902 datasheet (App. G); shelf = Classic99/MAME | Ch. 33 §33.1/§33.2 | R-12 (unemulated); termlib.inc assembles (entry >602E) but runs on shelf emulators/hardware, NOT this emulator/bench |
| PIO parallel printing (UNEMULATED) | the RS232 card's parallel port drives a printer: 8 data bits + strobe + busy line. DSR level = open "PIO" + WRITE formatted lines (file I/O). Line-printer conventions: CR/LF-terminated lines, form-feed pages, columns padded by hand — no page-description language. R-12: NOT emulated; describe + shelf tools | Ch. 33 §33.3 | R-12 (unemulated) |
| Cassette (UNEMULATED — explicit deferral) | the people's storage: programs saved as SOUND. The 9901 drives it — **motor relays CS1/CS2** (start/stop transport) + a tape-out line (data as an audio tone) + tape-in; **~1370 baud** frequency-shift encoding (tones for 0/1); the console **cassette DSR reached via GPLLNK** (GPL-side, Ch. 23/28). **R-12: NOT emulated** — cru.rs:134-139 "Decision (Joel, 2026-07-02): cassette stays unemulated for now" (CS1/CS2 + audio unwired; awaits the 9901 interval timer + a tape source). Modern trick: master as WAV/FLAC + load a real console from a phone's headphone jack (audio survives) | Ch. 33 §33.4/§33.5 | R-12 (unemulated, dated deferral cited from cru.rs); shelf = Classic99/MAME + audio files/hardware |
| Ch. 33 code artifacts | `code/ch33/`: termlib.inc (9902 CRU-serial: TINIT/TPUTC/TGETC, poll transmit-empty/receive-loaded) + termlib.a99 (assembles, entry >602E). Lab: TERM99 (mini terminal + XON/XOFF) + a cassette round-trip through a WAV — both run on shelf emulators/hardware, NOT this emulator. Exact 9902 CRU bits = datasheet/App. G (not asserted at fragile precision) | Ch. 33 Lab | assembles via `sh verify.sh` (57 sources); NOT runnable here (R-12 — RS232/cassette unemulated) |
| SAMS paged memory (UNEMULATED) | Super AMS = 1 MB+ RAM seen 4 KiB at a time through a MAPPER. The CPU's eight 4K expansion windows (>2000/>3000/>A000..>F000) each show a 4K PAGE of the big RAM, chosen by a mapper register. Model: **CRU base >1E00 bit0** enables mapper-register access (registers over the >4000 area); write a page number to the window's register to select its page. Enables far data (worlds >32K, streamed), bank-aware code, size detection. **R-12: NOT emulated** (fixed 32K; no mapper, no >1E00) — described from SAMS docs; shelf = MAME/js99er/hardware | Ch. 34 §34.3 | R-12 (unemulated); samslib.inc assembles (entry >6042) but runs on shelf/hardware, NOT this emulator |
| F18A enhanced VDP (UNEMULATED) | an FPGA TMS9918A REPLACEMENT: runs all stock modes + (after an UNLOCK register sequence) adds a full palette, more sprites (no 4/line law), **80-column text**, scanline effects, VGA/HDMI. Contains a **9900-class GPU inside the video chip** -> the console is secretly DUAL-PROCESSOR (programmed in earnest Ch. 44). **R-12: the project's VDP is a PLAIN TMS9918A** — no unlock/enhanced registers/GPU/80-col — described from F18A docs; shelf = MAME/js99er | Ch. 34 §34.4 | R-12 (VDP = plain 9918A, per vdp.rs); F18A GPU -> Ch. 44 |
| TIPI (Pi bridge, UNEMULATED) | a Raspberry Pi on a PEB card bridging the console to the modern world via a DSR + a register message-protocol (mailbox). Gives: a MAPPED FILESYSTEM (DSK-style names -> Pi files, via the PAB), and EXTENSION CALLS to TCP/web APIs -> **the internet from a 1981 console** (HTTP, sockets, online high scores). Needs defensive detection (absent on most consoles). **R-12: NOT emulated** (absent entirely) — described from TIPI docs; develop against real hardware | Ch. 34 §34.2 | R-12 (unemulated) |
| Compatibility doctrine + FinalGROM dev | **STOCK-FIRST, ENHANCE-IF-PRESENT**: write for the stock console (what everyone has + what the emulator VERIFIES), then DETECT each enhancement (a non-destructive probe a stock machine fails harmlessly — SAMS: does a page register change a window? F18A: unlock + read a signature; TIPI: probe its registers) and use it if present, degrade gracefully if not. Detection recipes = App. I. FinalGROM 99 DEV MODE = build-copy-run on real hardware (the back half of the emulator-first pipeline) | Ch. 34 §34.5/§34.6 | the emulator's stock fidelity IS the doctrine's baseline (R-12 discipline as design doctrine) |
| Ch. 34 code artifacts | `code/ch34/`: samslib.inc (SAMS mapper: SAMSEN/SAMSDI CRU gate + SAMSMP map a page into a window) + samslib.a99 (assembles, entry >6042). Lab: SAMS level-streamer on TERRAIN (world >32K, stock-32K fallback) + stretch TIPI online high-score for DODGE — both run on shelf/hardware, NOT this emulator | Ch. 34 Lab | assembles via `sh verify.sh` (58 sources); NOT runnable here (R-12 — SAMS/F18A/TIPI unemulated) |
| Cartridge = ROM at >6000 + >AA header | a cartridge = 8 KB ROM in the **>6000-7FFF** window + (for banked/hybrid) bank logic + maybe GROM. Introduces itself with the **>AA ROM header** (the machine-code sibling of Ch. 27's GPL header: >AA + version + program list + power-up list) — the master menu scan (Ch. 28) finds it, lists "n FOR NAME", launches a machine-code program via **XML >F0** (Ch. 29). libre99asm cartridge mode auto-generates the header pointing at START | Ch. 35 §35.1 | machine-verified (libre99asm builds discoverable carts; XML >F0 verified Ch. 29) |
| Bank-switching (write-to->60xx) | 8 KB is too small -> a cart carries many 8 KB BANKS, one visible at a time, selected by **WRITING to the >6000 window**: the TI scheme derives the bank from the write ADDRESS — **bank = (addr>>1) & (banks-1)** (value irrelevant; banks = power of 2). Write >6000+2N selects bank N. Schemes: 2-bank -> the 378/379 "inverted" multi-bank (32K->512K->MB, FinalGROM/megacarts) -> ROM+GROM hybrids -> RAM carts (Mini Memory) | Ch. 35 §35.2 | **machine-verified on BENCH99**: 2-bank bankcart, m 8320 = B0 B0 B1 B1 B0 B0 (write >6002 -> bank1, write >6000 -> bank0); core machine.rs write_cartridge/read_cartridge |
| Bank-safe design (the hazard + toolkit) | HAZARD: the bank-switch code LIVES IN A BANK — switching the bank you're executing in changes the code under the PC. Toolkit: **resident common segment** (identical code in every bank at the same addrs), **trampolines** (resident switch-and-call), cleanest = **run the switch from scratchpad RAM** (outside the window — the verified bankcart copies its probe to >8360 and runs there), per-bank vector tables, data banking (never execute from a bank you're about to swap). Rule: never switch the bank you're running in without a resident foothold | Ch. 35 §35.3 | machine-verified (bankcart's RAM-resident trampoline: B @>8360 before any switch) |
| Cart cold-start + build/ship | a cart boots into a COLD console (menu-launched, nothing set up): it must establish its own workspace (LWPI to pad), claim pad regions, bring the VDP up from cold, and NOT lean on E/A utilities (bring its own = lib99, or Ch. 23 shims). BUILD (R-12): libre99asm assembles ONE 8 KB bank; a multi-bank image = per-bank assembly CONCATENATED (raw; the bench auto-detects banks = len/8K) — a single-file .ctg/.rpk multi-bank packager is a ROADMAP item. Targets: .ctg (project), .bin (FinalGROM), .rpk (MAME), Classic99 | Ch. 35 §35.4/§35.5 | R-12 (libre99asm = single-bank build; concatenation verified via bankcart); cold-start = Ch. 23-24 |
| Ch. 35 code artifacts | `code/ch35/`: bankcart.a99 (bank 0: copies a PROBE to RAM, switches banks by writing >6002/>6000, publishes 3 markers) + bankcart1.a99 (bank 1, marker >B1B1). Concatenated = a 2-bank image. Lab: DODGE as a 32K 4-bank cart (resident kernel+lib99, banked music/levels) — described | Ch. 35 Lab | machine-verified (bench: B0 B0 B1 B1 B0 B0); assembles via `sh verify.sh` (60 sources) |
| Memory budgeting + VRAM warehouse + overlays | architecting a TI program starts with a BUDGET WORKSHEET (allocate before use): scratchpad (256 B, fast), expansion RAM (<=32 KB, main), VRAM (16 KB, port-accessed), GROM, cart banks (8 KB), disk — filled per archetype (cart action / disk RPG / productivity). **VRAM as a data WAREHOUSE**: park COLD data (maps/tables/spare graphics — brilliant) but NEVER hot per-frame data (port cost = trap). Hierarchy: hot in scratchpad, warm in expansion, cold in VRAM/disk. **OVERLAYS from disk**: resident core (lib99 + loader + state) always present; phase-specific code/data loaded on demand (filelib) -> a program exceeds RAM | Ch. 36 §36.1/§36.2/§36.3 | architecture (memories = Ch. 5/24; overlays = Ch. 31 filelib) |
| Entity/state architecture (SKELETON99) | two verified patterns: **fixed-slot ENTITY TABLE** (a dense array of fixed slots — SKELETON99: 8 slots x 8 bytes [x.hi x.lo y.hi y.lo dx dy type live]; iterated tight by STEP, skipping dead slots — the ECS ancestor, chosen for speed + fixed memory, no allocation) and a **game-STATE MACHINE** (a STATE var + a jump table PTAB dispatching title/play/over each frame). Data-driven: the PTAB dispatch + the generic STEP-over-data | Ch. 36 §36.4/§36.6 | **machine-verified BENCH99**: STATE=0 -> TITLE SIG=>7171, STATE=1 -> PLAY SIG=>5050; STEP advances a live entity |
| Fixed-point doctrine (8.8) | no FP on the 9900 -> games use FIXED-POINT. House standard = **8.8** (8 int + 8 fraction/256ths; >0140 = 1.25; add a velocity = plain A; the pixel = the high byte). 12.4 = more range/less precision; 4.12 = more precision/less range. **256-unit byte-angles** (wrap free via a byte add) index a precomputed **SINE TABLE** (sin lookup + FXMUL, no runtime trig; cos = sin+64) = the motion toolkit | Ch. 36 §36.5 | **machine-verified BENCH99**: SKELETON99 STEP, x=1.00(>0100) + dx=0.25(>40) -> x=1.25(>0140); fxcalc FXMUL (Ch. 23) |
| Saving state (3 tiers) | persistence by storage present: **disk** (Ch. 31 — high scores/config as DV80/INTERNAL, saves as PROGRAM/record files via filelib), **cassette** (Ch. 33 — a tape block), or **NO storage** -> computed-and-verified **PASSWORDS/codes** (encode progress into a short code the player writes down + types back; a checksum/light cipher rejects invalid codes). Every finished game persists something | Ch. 36 §36.7 | architecture (disk = Ch. 31, cassette = Ch. 33) |
| XB CALL LINK ABI (the one fenced XB page) | half the commercial catalog = XB + an assembly core via **CALL LINK("NAME",args...)**. The assembly side: read numeric/string args by number via XB's NUMREF/STRREF utilities, compute in 9900 code, write results via NUMASG/STRASG, preserve XB's environment, RT. = the Ch. 29 shell+core hybrid with XB as the shell. **The book's ONLY XB content (ABI only, no XB taught)** | Ch. 36 §36.8 | reference (the one fenced XB allowance per the book scope) |
| Ch. 36 code artifact (SKELETON99) | `code/ch36/skeleton99.a99` = the PROJECT TEMPLATE all Part IX instantiates: phase state machine (PTAB dispatch title/play/over) + fixed-slot entity table (8x8) + STEP (8.8 motion) + lib99-resident. Phase bodies = signature stubs (the chassis, not a full game) | Ch. 36 Lab | machine-verified (bench: dispatch SIG 7171/5050 + STEP 0100->0140); assembles via `sh verify.sh` (61 sources) |
| Optimization doctrine + wait-state ledger | order: **MEASURE (Ch. 22 cycles/Amdahl) -> MOVE MEMORY (placement) -> CHOOSE INSTRUCTIONS**, by return-on-effort. Wait-state ledger (bus.rs): **scratchpad (>8000-83FF) + console ROM (>0000-1FFF) = 0 wait** (native 16-bit); everything else (expansion RAM, cartridge >6000, VDP) = **~4 cycles/access** (8-bit multiplexer); **GROM ports +13-22** more. Cartridge ROM is NOT fast memory | Ch. 37 §37.1/§37.2 | machine-verified (bus.rs wait_states) |
| PLACEMENT IS KING (measured) | keep hot data/code in 0-wait memory. **Workspaces ALWAYS in scratchpad** (every register access hits the WS). VERIFIED: the SAME 100-iter loop = **2818 cycles (WS in scratchpad >8300)** vs **3622 cycles (WS in expansion >2000)** = **+804 cycles, ~29%** (~8/iter = 2 WS accesses x 4 waits). Hot loops copied into pad RAM (0-wait fetch) | Ch. 37 §37.2 | **machine-verified BENCH99** (placebench.a99: wp 8300 -> 2818, wp 2000 -> 3622) |
| Instruction-level + MPY/DIV deviation (R-15) | strength reduction (x by 2^n -> shift; x40 = (x<<5)+(x<<3)), flag-riding (branch on a side-effect status), fallthrough loops. **R-15 DEVIATION**: real hardware MPY ~52 / DIV ~92-124 cyc vs a shift ~16 -> AVOID MPY/DIV; but the **emulator models MPY/DIV FLAT** (cpu.rs, matching Classic99; an MPY-x2 loop + an SLA-x2 loop both measured **5628** on the bench) -> cite the DATASHEET (App. A), not the bench, for MPY/DIV | Ch. 37 §37.3 | machine-verified deviation (bench MPY=SLA=5628); hardware costs = datasheet (R-15) |
| Unrolling/SMC/VDP/table-ism | unrolling (Nx body/branch — bigger code = slower fetches, can cancel), jump-table vs chained-compares (measured crossover). SMC = RAM-ONLY (silently fails on ROM — loud warnings). VDP throughput = AMORTIZATION (set the address once + stream via VMBW auto-increment; never byte-at-a-time in a hot path). TABLE-ISM: precompute off-machine (Ch. 38), runtime = a lookup | Ch. 37 §37.4-§37.7 | wait-state-driven |
| Scroller case study + when to stop | §37.8: Ch. 17's scroller tuned **65% -> 31% of frame** decision-by-decision (placement -> VDP batching -> strength-reduction -> unroll), each measured. §37.9 STOP when: the frame fits / not worth it / SHIP. "Fast enough, correct, shipped" > "maximally fast" | Ch. 37 §37.8/§37.9 | method |
| Ch. 37 code artifact | `code/ch37/placebench.a99` = the placement proof (a 100-iter loop with NO self-LWPI so the bench points WP at scratchpad vs expansion + measures). Lab: the optimization dojo (5 routines, target cycle counts, cycles = the scoreboard) | Ch. 37 Lab | machine-verified (2818 vs 3622); assembles (62 sources) |
| Asset pipeline (two-machine workflow) | a game is mostly DATA (graphics/levels/music/text/tables) -> it must be GENERATED + COMPRESSED + BUDGETED. HOST (modern computer) runs generators (converters/compressors/table builders = the conveyor belt); TARGET (TI) runs lean consumers (decompressors/loaders). **R-12**: the outline imagines make+python, but the PC has NEITHER (repo policy = sh+cargo only) -> the pipeline is libre99asm/libre99gpl + sh scripts + small cargo converters; python-heavy IMAGE conversion runs on the MAC (like Ch. 6). Principle machine-independent; implementation honors sh+cargo | Ch. 38 §38.1/§38.2 | R-12 (sh+cargo pipeline; python image work on the Mac) |
| Compression at 3 MHz (RLE verified) | compression = a space-vs-decode-time TRADE; match the scheme to the data + keep the decoder small/fast (it runs on the target). **RLE** (PackBits: C=0 end, C>=>80 run of C&>7F copies of the next byte, C<>80 literal of C bytes) = the workhorse for run-heavy data. **LZ77-family** (literals + back-references [copy L from D back]) for repeated substrings (better ratio, bigger/slower decoder — measured). **5-bit text packing** (+digraphs) for prose (37.5%+ off) | Ch. 38 §38.3 | **machine-verified BENCH99**: rlelib decodes >85 >41 / >03 >42 >43 >44 / >00 -> "AAAAABCD" (41 41 41 41 41 42 43 44 @ >2000); LZ/text-packing described |
| Data schemas + single source of truth | a generated asset = a binary blob + a LAYOUT; the generator (emits) + the 9900 code (reads) must agree EXACTLY or the game reads garbage (drift bug). Fix: generate BOTH the binary AND the matching **EQU header** from ONE schema definition (the parser's offsets = the generator's layout by construction); version the blob (a version byte) for migration. §38.6: automated BUDGET ACCOUNTING (the build sums asset sizes vs the Ch. 36 worksheet -> red ink at build time, not a run-time crash) | Ch. 38 §38.5/§38.6 | the single-source-of-truth discipline (protobuf-style) at TI scale |
| Ch. 38 code artifacts | `code/ch38/`: rlelib.inc (RLE decompressor — RLEDEC) + rledec.a99 (self-test -> "AAAAABCD"). Music (Ch. 19 VGM/tracker -> note streams, PSG plays + bench-verified) + speech (Ch. 20 LPC frames, synth unemulated R-12) integrate into the same build. Lab: crunch a 24K asset pack -> a 12K bank + a 600-byte resident decompressor (a 2:1 squeeze, the budget report proves it) | Ch. 38 Lab | machine-verified (bench: RLE -> AAAAABCD); assembles via `sh verify.sh` (63 sources) |

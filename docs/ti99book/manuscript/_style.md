# `_style.md` — Distilled Style Guide and Accrued Rulings
*Derived from Master Outline §3. New rulings are appended with the chapter that set them. This file is loaded at the start of every writing session.*

## Core conventions (from the outline v1.1, binding)
- Hex: TI `>` prefix (`>8300`, `>AA`). Registers `R0`–`R15`; `WP`, `PC`, `ST`.
- Mnemonics/directives UPPERCASE. Classic listings: labels ≤6 chars; longer labels flagged `[libre99asm]`.
- Assembler baseline: E/A-compatible source, assembled with **libre99asm** (the host project's assembler). GPL: **libre99gpl**. Divergences from E/A flagged `[libre99asm]` (R-13). xdt99 = interchange toolchain (Ch. 6 period formats, Part VII disks).
- K = 1024. Decimal unless `>`-prefixed.
- Diagrams: Markdown tables + labeled ASCII art only (Ruling R-5, below).
- Code fences: ```asm, ```gpl, ```text, ```sh/```make, ```python.
- Hardware baseline "standard system" = console + 32K + disk. Badges: `[console-only]`, `[cart]`.
- Emulators: the **libre99 project** primary (desktop app + BENCH99); Classic99/js99er/MAME shelf equivalents noted where they differ (R-12).
- Tone: rigorous but warm. Humor in vignettes/sidebars only. American English.

## Chapter file template (Ruling: Ch. 1)
1. `# Chapter N — Title` + one-line italic epigraph/scope note + part/page-target comment line.
2. Opening vignette: unnumbered `##` with an evocative title; ends with `---`.
3. `## What You Will Learn` — bulleted, testable objectives.
4. `## The Bridge: ...` — modern-to-vintage mapping, 1–3 paragraphs.
5. Numbered body sections `## N.k Title`; subsections `###` (unnumbered).
6. Boxed matter as blockquotes with bold tags:
   - `> **Sidebar — Title.**`
   - `> **Field Notes — Title.**`
   - `> **Pitfalls.**` (in history chapters this box is titled **Myths and Misconceptions**)
7. `## Lab N — Title` (running artifact; history chapters may substitute a guided activity).
8. `## Exercises` — numbered `N.k` with tier marks ✦ / ✦✦ / ✦✦✦.
9. `## Further Reading`.
10. `## Summary` — the 6–10 line block that doubles as context priming; copied verbatim into `_summaries.md`.

## Accrued rulings
- **R-1 (Ch. 1):** Any invented period artifact (memo, ad, price sheet) must be explicitly labeled a *reconstruction* or *composite* in its first line. We never present fabricated documents as authentic.
- **R-2 (Ch. 1):** Dollar figures, unit counts, and corporate financials from period reporting are hedged ("reported," "roughly," "commonly cited") unless traceable to a primary source. First assertion of each figure is logged in `_ledger.md`; later chapters must reuse the logged form.
- **R-3 (Ch. 1):** People are named where the historical record names them; engineering credit is attributed carefully and never invented.
- **R-4 (Ch. 1, provisional pending author sign-off):** Working title stands; case-study placeholder names (METEOR BELT, GRIDRUNNER 99, DUNGEONS OF FATE, AUTHOR99) stand.
- **R-5 (Ch. 1, provisional):** PAL coverage = sidebars only; diagrams = ASCII/tables only (no image assets).
- **R-6 (Ch. 1):** Em dashes are set closed (word—word) in body prose? **No** — this manuscript sets them open with spaces (word — word) for readability in monospace renderers. Applied from Ch. 1 onward.
- **R-7 (Ch. 1):** Cross-references use the form "(Ch. 12)" or "(§17.5)"; forward references are welcome and should promise, not spoil ("we will measure this claim in Ch. 22").
- **R-8 (Ch. 2):** Named recurring artifacts and metaphors are canon vocabulary once logged in `_ledger.md` (e.g., **CQ-82** checklist, **the tower** of interpreters, **the funnel/mail slot** for multiplexer/VDP ports, **the fast island/domain**, the law "**speed is a property of addresses**"). Later chapters reuse the handle rather than re-coining.
- **R-9 (Ch. 3):** Shell commands are shown POSIX-style, no prompt character, one command per line; Windows deltas noted in parentheses only where they differ materially. Tool names include the `.py` suffix (`xas99.py`) in commands, bare (`xas99`) in prose. *(v1.1: the `.py` clause applies to the xdt99 tools where they appear — Ch. 6/Part VII; the project binaries `libre99asm`/`libre99gpl`/`bench99` carry no suffix anywhere.)*
- **R-10 (Ch. 3):** Canonical assembler invocation is `xas99.py -R` (+ `-i`/`-b` per artifact, `-L` for anything debugged). All listings in the book assume `-R`. Makefile variable names (`XAS`, `XDM`, `NAME`) are canon; labs reuse the Ch. 3 skeleton verbatim. **[Superseded by R-14 in the v1.1 re-founding; retained as the record of the xas99-era sessions. xas99 remains the Ch. 6 interchange tool, invoked per this ruling there.]**
- **R-11 (Ch. 4):** Exercises may supply datasheet-derived constants "as premises (audited in App. A)" before the appendix is drafted, keeping chapters self-contained without unverified assertions in body prose. Body prose asserts only ledgered figures.
- **R-12 (v1.1 re-founding):** The **libre99 project** (the repository this book lives in) is the book's foundation. Canonical names: the desktop app is `libre99` ("the project emulator" in prose), the assembler `libre99asm`, the GPL toolchain `libre99gpl`, the lab bench **BENCH99** (`code/bench/`), the core crate `libre99-core`. Classic99, js99er.net, MAME, and xdt99 are first-class *discussed* tools with fixed shelf roles (GUI debugger + licensed bundle + Ch. 6 period workflow; zero-install/speech/F18A; reference accuracy; period-format & disk interchange). Chapters must not assert project capabilities beyond what ships at HEAD when the chapter is written; a gap is stated plainly and the shelf tool that covers it is named. Emulator usage details (hotkeys, flags, file locations) are cited to the project README rather than duplicated at fragile precision.
- **R-13 (v1.1):** The extension flag `[xas99]` is retired in favor of **`[libre99asm]`** — same meaning: a construct beyond strict Editor/Assembler that our baseline accepts. Existing occurrences were updated with the re-founding. Constructs specific to xdt99 in Ch. 6/Part VII interchange contexts are flagged `[xas99]` there, on purpose.
- **R-14 (v1.1, supersedes R-10):** Canonical build: `libre99asm <src>.a99 --name '<Title>' -o build/<name>.ctg --listing build/<name>.lst --symbols build/<name>.map.json`, plus `--format bin -o build/<NAME>C.bin` for the universal ROM image. The lab skeleton is `src/` + `build/` + `build.sh` with variables `LIBRE99ASM`, `NAME` (TI-lawful, ≤10 chars uppercase), `TITLE` (the menu line). Listings and symbol maps are always generated for anything debugged. The book repo's own `setup.sh`/`verify.sh` (sh + cargo only — no make/python dependency) are the verification protocol.
- **R-15 (v1.1):** Program entry is the label **`START`** by convention — libre99asm's default entry symbol (or `END`'s operand / `--entry`); `DEF` belongs to the E/A loader world and appears from Ch. 6 on. Trailing comments on **no-operand** mnemonics require `;` `[libre99asm]`. "Machine-verified" in the ledger now means verified against the project toolchain/emulator at a stated commit; where the emulator deviates from hardware references (datasheet/Classic99/MAME), body prose asserts the *hardware* truth and the deviation gets its own ledger row.
- **R-16 (Ch. 9):** The book-wide **calling convention**, stated once and obeyed from here on (every `lib99` routine, Ch. 11+). **R10 is the software stack pointer**, full-descending (grows toward lower addresses, points *at* the top item), initialized once to a reserved scratchpad top: **PUSH x = `DECT R10` / `MOV x,*R10`**; **POP x = `MOV *R10+,x`**. Arguments and results ride **R0–R2**, caller-saved scratch (byte cargo in the high half, per the high-byte law). A **leaf** (calls nothing) may keep its return in R11 and return `RT` (= `B *R11` = >045B); a **non-leaf** PUSHes R11 in its prologue and POPs before `RT`, and PUSHes/POPs any register it must preserve across an inner call. **R10 and R13–R15** (the BLWP linkage) are never used as scratch. **Errors are reported in R0** (>0000 = success, nonzero = code; caller tests `MOV R0,R0` / `JNE`), generalizing Ch. 8's DIVSAF (§9.9). Later chapters cite R-16 rather than restating it.
- **R-17 (Ch. 11):** The **include architecture and `lib99` conventions**. `equates.inc` names every magic console address once; shared source is pulled in with `COPY 'file'`, which `libre99asm` resolves **relative to the directory of the source file being assembled** (a sibling include is therefore a bare filename, `COPY 'equates.inc'`); an EQU-only include takes a non-`.a99` extension (`.inc`) so `verify.sh`'s `*.a99` sweep skips it. Programs are *composed* by `COPY`, not linked (no `DEF`/`REF` on our baseline), so all names share one flat namespace and the §11.1 naming discipline is load-bearing. **`lib99`** (`code/lib99/`, born this chapter — its README is the contract) is the book's accumulating library: each module is one file obeying R-16, with a header, a per-routine register-use map, addresses via `equates.inc`, and a `START` self-test in the `mathlib` single-file pattern (assembles stand-alone under `verify.sh`, proves itself on BENCH99, companion `.bench` asserts the verdict). The library is versioned by the book — it grows one chapter at a time. `memlib` (Ch. 7) and `mathlib` (Ch. 8) are its founding modules; later chapters add to it and cite R-17.
- **R-18 (Ch. 14):** The **Part III graphics-verification standard.** A graphics claim is proven on BENCH99's `pixels [step]` framebuffer view — it renders the real 256×192 output through the VDP rasterizer and prints one palette-index hex digit per sampled pixel (default step 4 → 64×48, so one glyph maps to one multicolor fat pixel) — *alongside* `vram` (the bytes landed) and `vdp` (the registers). `vram`/`vdp` prove the tables and registers; `pixels` proves they paint the intended picture, and is the strongest graphics evidence tier. `screen` is mode-aware (40 columns in text mode, 32 otherwise). Palette indices 0 (transparent) and 1 (black) share an RGB and both read `0` in `pixels` — an ambiguity that never matters for shape/colour checks. The border-verdict light (GREEN >02 / RED >06, Ch. 7) remains the pass/fail summary a `.bench` script asserts; `pixels` is the picture proof behind it. Added to BENCH99 at commit 0d3e5d5 (with the `vram` oracle of Ch. 12, R-12-clean: uses only libre99-core's public API).
